---
phase: 03-code-review-diff-editing
plan: 08
type: execute
wave: 4
depends_on: ["03-03", "03-05", "03-06"]
files_modified:
  - frontend/src/components/review/ReviewChat.tsx
  - frontend/src/components/review/ReviewAnnotations.tsx
  - frontend/src/components/board/TaskCard.tsx
  - frontend/src/hooks/useReviewChat.ts
autonomous: true

must_haves:
  truths:
    - "User can open a contextual chat panel alongside the review"
    - "Chat has full context of the review (diff, annotations, existing comments)"
    - "User can highlight text in the diff and add as context in the chat"
    - "MR/PR task cards on the board have special actions: View diff, Send for AI review, Approve, Request changes"
  artifacts:
    - path: "frontend/src/components/review/ReviewChat.tsx"
      provides: "Contextual chat interface for reviews"
      min_lines: 80
    - path: "frontend/src/components/review/ReviewAnnotations.tsx"
      provides: "Annotation list with batch operations"
      min_lines: 40
    - path: "frontend/src/hooks/useReviewChat.ts"
      provides: "Chat messaging hook"
      exports: ["useReviewChat"]
  key_links:
    - from: "frontend/src/components/review/ReviewChat.tsx"
      to: "frontend/src/stores/reviewStore.ts"
      via: "chat messages state"
      pattern: "useReviewStore"
    - from: "frontend/src/hooks/useReviewChat.ts"
      to: "backend/app/api/ai_review.py"
      via: "POST /api/review/chat"
      pattern: "api/review/chat"
    - from: "frontend/src/components/board/TaskCard.tsx"
      to: "frontend/src/stores/sessionStore.ts"
      via: "openTab for diff/review"
      pattern: "openTab"
---

<objective>
Build the contextual review chat panel, wire annotation batch operations, and add MR/PR-specific actions to task board cards.

Purpose: The chat panel gives users a Claude-powered conversation about the code review -- they can ask about comments, draft responses, and discuss findings. The board card actions (View diff, Send for AI review, Approve, Request changes) connect the Phase 2 task board to the Phase 3 diff/review system, completing the user flow: Feed -> Board -> Review.

Output: ReviewChat panel with diff/annotation context, ReviewAnnotations standalone component, MR/PR action buttons on TaskCard.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-review-diff-editing/03-RESEARCH.md
@.planning/phases/03-code-review-diff-editing/03-03-SUMMARY.md
@.planning/phases/03-code-review-diff-editing/03-05-SUMMARY.md
@.planning/phases/03-code-review-diff-editing/03-06-SUMMARY.md
@frontend/src/stores/reviewStore.ts
@frontend/src/stores/sessionStore.ts
@frontend/src/components/board/TaskCard.tsx
@frontend/src/components/diff/AnnotationPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review chat panel + chat API endpoint</name>
  <files>
    frontend/src/components/review/ReviewChat.tsx
    frontend/src/hooks/useReviewChat.ts
    backend/app/api/ai_review.py
  </files>
  <action>
Add to backend/app/api/ai_review.py:

`POST /api/review/chat` - Body: `{ messages: list[dict], context: str }`. Calls ai_review_service.chat_about_review(). Returns `{ response: str }`. The context string includes the diff text, annotations, and existing comments -- the frontend builds this string.

Create frontend/src/hooks/useReviewChat.ts:
- `useReviewChat()` returns `{ sendMessage, isLoading }`
- `sendMessage(userMessage: string, context: { diffText: string, annotations: ReviewAnnotation[], comments: CommentThread[], selectedText?: string })`:
  - Build context string from the review data (diff text, annotation summaries, comment thread summaries)
  - If selectedText provided, prefix with "Regarding this code:\n```\n{selectedText}\n```\n"
  - Get conversation history from reviewStore.reviewChatMessages
  - POST to /api/review/chat with messages array and context string
  - On success: add both user message and assistant response to reviewStore.addChatMessage

Create frontend/src/components/review/ReviewChat.tsx:
- Floating resizable side panel that slides in from the right edge (per locked decision)
- Shown when reviewStore.chatPanelOpen is true
- Positioning: fixed right, overlaying part of the diff area (NOT pushing content)
- Width: 380px default, resizable via drag handle on left edge (use a simple onMouseDown/onMouseMove/onMouseUp handler -- no library needed)
- Min width: 300px, max width: 600px
- Header: "Review Chat" title, close button (X)
- Body: scrollable message list
  - User messages: right-aligned, bg-accent/10 rounded bubble
  - Assistant messages: left-aligned, bg-secondary rounded bubble, rendered as plain text with code blocks formatted in monospace
- Input area at bottom:
  - Textarea (auto-resize, max 4 rows)
  - "Send" button (or Enter to send, Shift+Enter for newline)
  - "Add context" hint text when selectedText is available
- Context indicator: if the user has selected text in the diff, show a small banner above the input: "Selected code will be included as context" with a dismiss button
- Text selection from diff: listen for mouseup events on the diff container, check window.getSelection(), if text selected, store in a local state `selectedDiffText`. Show the context indicator. Clear on chat panel close.
- Loading state: show typing indicator (...) while waiting for response
  </action>
  <verify>
    Run: `cd /c/Users/USER/locus && python -c "from app.api.ai_review import router; routes = [r.path for r in router.routes if hasattr(r, 'path')]; print(routes)"` -- should include /chat.
    Run: `cd /c/Users/USER/locus/frontend && npx tsc --noEmit 2>&1 | head -20` -- no errors.
  </verify>
  <done>Review chat panel opens from right edge as floating resizable overlay. Chat has full context of diff, annotations, and comments. User can highlight diff text and include as context. Messages sent to Claude API via backend, responses displayed as conversation.</done>
</task>

<task type="auto">
  <name>Task 2: MR/PR task card actions + ReviewAnnotations component</name>
  <files>
    frontend/src/components/review/ReviewAnnotations.tsx
    frontend/src/components/board/TaskCard.tsx
  </files>
  <action>
Create frontend/src/components/review/ReviewAnnotations.tsx:
- Standalone annotation list component (reusable outside AnnotationPanel)
- Props: `{ annotations: ReviewAnnotation[], onEdit: (id, text) => void, onToggleSelect: (id) => void, selectedIds: Set<string> }`
- Renders each annotation as a compact card:
  - Checkbox (checked if in selectedIds)
  - Severity badge (colored pill: error=red, warning=amber, suggestion=blue, info=gray)
  - File:line reference (text-xs)
  - Editable comment textarea
- "Select All" / "Deselect All" toggle at top
- Used by AnnotationPanel (from Plan 06) -- update AnnotationPanel to use this component instead of duplicating the list rendering

Update frontend/src/components/board/TaskCard.tsx:
- Detect if a task is associated with an MR/PR: check if task.feed_item_id exists AND the linked feed item's source_type is "github" or "gitlab" (the source_type info should be available from the task's source_links or the feed item). Use a heuristic: if task.source_links has a URL matching github.com or gitlab.com patterns, or if the feed item raw_payload has "pull_request" or "merge_request" keys.
- For MR/PR tasks, add a row of action buttons below the existing card content:
  - "View Diff" button (GitCompareArrows icon): opens a diff tab via sessionStore.openTab with type "diff" and mrId from the task/feed item data. Set diffData.mrId and diffData.sourceType. These fields come from the CenterTab.diffData type defined in Plan 03's sessionStore refactor.
  - "AI Review" button (Bot icon): opens diff tab AND triggers AI review (opens annotation panel)
  - "Approve" button (Check icon, green): calls useApprove mutation from useReviewApi
  - "Request Changes" button (X icon, red): opens ReviewSubmitDialog with event pre-set to REQUEST_CHANGES
- These buttons are styled as small icon buttons in a row, only visible when the card is expanded or hovered
- Non-MR/PR tasks do not show these buttons

Wire the "View Diff" action: when clicked, extract the MR/PR identifier from the task's feed item. The feed_item.raw_payload should contain the PR/MR number, owner/repo (GitHub) or project_id (GitLab). Create a diff tab with mrId set. The diff tab, when active, uses useMrDiff(taskId) to fetch the diff from the review API.
  </action>
  <verify>
    Run: `cd /c/Users/USER/locus/frontend && npx tsc --noEmit 2>&1 | head -20` -- no errors.
    TaskCard should show action buttons for MR/PR-linked tasks (View Diff, AI Review, Approve, Request Changes).
  </verify>
  <done>ReviewAnnotations is a reusable editable annotation list component. MR/PR task cards show View Diff, AI Review, Approve, and Request Changes action buttons. View Diff opens an MR/PR diff tab in the center panel.</done>
</task>

</tasks>

<verification>
- Review chat panel opens from right edge overlay
- Can send messages and receive responses (with LLM key configured)
- Text selection in diff adds context to chat
- MR/PR task cards show four action buttons
- "View Diff" opens MR/PR diff in center panel
- ReviewAnnotations component renders with checkboxes and editable text
</verification>

<success_criteria>
Contextual chat panel provides Claude-powered conversation about reviews with full diff/annotation/comment context and text selection support. MR/PR task cards on the board provide direct actions (View Diff, AI Review, Approve, Request Changes) connecting the board workflow to the review surface.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-review-diff-editing/03-08-SUMMARY.md`
</output>
