---
phase: 03-code-review-diff-editing
plan: 04
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - frontend/src/components/diff/DiffViewer.tsx
  - frontend/src/components/diff/DiffFileList.tsx
  - frontend/src/components/diff/DiffToolbar.tsx
  - frontend/src/components/diff/DiffContextBar.tsx
  - frontend/src/hooks/useDiffData.ts
autonomous: true

must_haves:
  truths:
    - "User can view diffs in split (side-by-side) view by default"
    - "User can toggle between split and unified diff views, preference is remembered"
    - "Diff has full syntax highlighting on both sides (language-aware)"
    - "Full file is displayed with changed sections highlighted -- no collapsed context regions"
    - "Virtual scrolling renders only visible lines for large diffs"
    - "Changed file list sidebar shows on the left with status icons (added/modified/deleted)"
    - "Local diffs show branch/repo context bar; MR/PR diffs show different context"
  artifacts:
    - path: "frontend/src/components/diff/DiffViewer.tsx"
      provides: "Full @git-diff-view/react integration replacing Phase 2 manual renderer"
      min_lines: 60
    - path: "frontend/src/components/diff/DiffFileList.tsx"
      provides: "Changed file list sidebar with status icons"
      min_lines: 30
    - path: "frontend/src/components/diff/DiffToolbar.tsx"
      provides: "Toolbar with split/unified toggle"
      min_lines: 20
    - path: "frontend/src/components/diff/DiffContextBar.tsx"
      provides: "Context header bar (local vs MR/PR)"
      min_lines: 20
    - path: "frontend/src/hooks/useDiffData.ts"
      provides: "TanStack Query hooks for diff fetching"
      exports: ["useDiffData", "useChangedFiles"]
  key_links:
    - from: "frontend/src/components/diff/DiffViewer.tsx"
      to: "@git-diff-view/react"
      via: "DiffView component"
      pattern: "DiffView"
    - from: "frontend/src/components/diff/DiffViewer.tsx"
      to: "@git-diff-view/file"
      via: "DiffFile constructor"
      pattern: "new DiffFile"
    - from: "frontend/src/hooks/useDiffData.ts"
      to: "backend/app/api/git.py"
      via: "fetch /api/git/diff"
      pattern: "api/git/diff"
---

<objective>
Replace the Phase 2 manual DiffViewer with a full @git-diff-view/react integration supporting split/unified views, syntax highlighting, virtual scrolling, file list sidebar, and context bars.

Purpose: The existing DiffViewer manually renders colored text lines with no syntax highlighting, no split view, and no virtual scrolling. This plan replaces it entirely with the @git-diff-view/react library that provides all of these features plus the widget system needed for inline annotations (Plan 06).

Output: Production-quality diff viewer with split/unified toggle, syntax highlighting, file list sidebar, and context-aware headers.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-review-diff-editing/03-RESEARCH.md
@.planning/phases/03-code-review-diff-editing/03-03-SUMMARY.md
@frontend/src/components/diff/DiffViewer.tsx
@frontend/src/stores/sessionStore.ts
@frontend/src/hooks/useApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace DiffViewer with @git-diff-view/react + create hooks</name>
  <files>
    frontend/src/components/diff/DiffViewer.tsx
    frontend/src/hooks/useDiffData.ts
  </files>
  <action>
Create frontend/src/hooks/useDiffData.ts with TanStack Query hooks:

1. `useDiffData(machineId, repoPath, filePath?, commitSha?)` - Fetches diff text from the existing /api/git/diff or /api/git/commit-diff endpoints. Returns `{ diffText, isLoading, error }`. Reuse the query logic from the old DiffViewer (try unstaged then staged for file diffs, single fetch for commit diffs).

2. `useChangedFiles(machineId, repoPath)` - Fetches the list of changed files from /api/git/changed-files endpoint (already exists from Phase 2). Returns `{ files, isLoading }` where files is `Array<{path: string, status: string}>`.

3. `useMrDiff(sourceType, mrId, projectInfo)` - Placeholder hook for MR/PR diffs (Plan 05 will implement the actual backend). For now, returns empty state. This lets the DiffViewer accept MR diff data without breaking.

COMPLETELY REPLACE frontend/src/components/diff/DiffViewer.tsx:

Delete all existing code (the manual DiffLine renderer). Replace with @git-diff-view/react:

```typescript
import { DiffView, DiffModeEnum } from "@git-diff-view/react";
import { DiffFile } from "@git-diff-view/file";
import "@git-diff-view/react/styles/diff-view.css";
```

New DiffViewer component:
- Props: `{ machineId, repoPath, filePath?, commitSha?, mrId?, sourceType?, diffText?: string, extendData?, renderExtendLine?, renderWidgetLine?, onAddWidgetClick?, isMrDiff?: boolean }`
- Use useDiffData hook when diffText is not provided directly
- Build DiffFile instance with useMemo (keyed on diffText + fileName):
  - `new DiffFile(fileName, "", fileName, "", [rawDiffText], language, language)`
  - Call `initTheme("dark")`, `initRaw()`, `buildSplitDiffLines()`, `buildUnifiedDiffLines()`
  - Detect language from file extension using a simple mapping function
- State: `mode` (DiffModeEnum.Split default), persisted to localStorage as "locus-diff-mode" per user decision (remember preference)
- Render DiffView with:
  - `diffFile={diffFile}`
  - `diffViewMode={mode}`
  - `diffViewTheme="dark"`
  - `diffViewHighlight={true}` (syntax highlighting)
  - `diffViewWrap={false}`
  - `diffViewFontSize={13}`
  - `diffViewAddWidget={isMrDiff}` (only show "+" button on MR/PR diffs per locked decision)
  - Pass through extendData, renderExtendLine, renderWidgetLine, onAddWidgetClick (these are used by Plan 06 for annotations)
- Loading, error, and empty states similar to before but styled consistently
- The DiffView component from @git-diff-view/react handles virtual scrolling internally -- no extra config needed

For multi-file diffs (commit diffs that contain multiple files): split the diff text by `diff --git` headers, render a DiffFile per file. The selectedFile state controls which file's DiffFile is rendered. This connects with DiffFileList from Task 2.
  </action>
  <verify>
    Run: `cd /c/Users/USER/locus/frontend && npx tsc --noEmit 2>&1 | head -20` -- no errors in DiffViewer or useDiffData.
    Open the app, navigate to a repo with changes, click a changed file -- should see a split diff view with syntax highlighting instead of the old colored text.
  </verify>
  <done>DiffViewer uses @git-diff-view/react with DiffFile instances. Split view is default, toggle persisted to localStorage. Full syntax highlighting on both sides. Virtual scrolling works for large diffs.</done>
</task>

<task type="auto">
  <name>Task 2: DiffFileList + DiffToolbar + DiffContextBar</name>
  <files>
    frontend/src/components/diff/DiffFileList.tsx
    frontend/src/components/diff/DiffToolbar.tsx
    frontend/src/components/diff/DiffContextBar.tsx
  </files>
  <action>
Create frontend/src/components/diff/DiffFileList.tsx:
- A vertical file list shown on the left side of the diff tab content area
- Uses useChangedFiles hook to get the list of changed files for the repo
- Each entry shows: status icon (green Plus for added, yellow Pencil for modified, red Trash2 for deleted), file name (just the basename), full path as tooltip
- Clicking a file updates the selected file in the diff view (via a callback prop or local state)
- Currently selected file is highlighted with bg-hover
- Width: fixed ~200px with overflow-x hidden, text-ellipsis for long names
- For MR/PR diffs: receives file list as prop (from the MR diff response) instead of useChangedFiles
- Shows file count at top: "N files changed"
- Compact styling: text-xs, tight padding, matching the sidebar aesthetic

Create frontend/src/components/diff/DiffToolbar.tsx:
- Horizontal toolbar at the top of the diff content area (below the tab bar, above the actual diff)
- Contains:
  - Split/Unified toggle: two buttons with GitCompareArrows (split) and ListTree (unified) icons. Active mode has accent background. Clicking changes the mode and saves to localStorage.
  - "Review with Claude" button (placeholder -- just a button that does nothing yet, Plan 06 will wire it). Show Bot icon + "Review with Claude" text. Disabled style for now.
  - "Approve" button (placeholder, visible only when isMrDiff=true)
  - "Request Changes" button (placeholder, visible only when isMrDiff=true)
- Height: ~36px, bg-secondary, border-b border-border

Create frontend/src/components/diff/DiffContextBar.tsx:
- A narrow context bar below the toolbar showing diff context information
- Two modes based on `isMrDiff` prop:
  - Local diff: Show "repo-name" > "branch-name" > "file-path" as breadcrumb segments
  - MR/PR diff: Show "MR !123: Title" or "PR #123: Title" with author avatar placeholder and status badge (open/merged/closed)
- Height: ~28px, bg-dominant, text-xs, text-muted
- Breadcrumb segments are clickable but do nothing yet (future: navigate to file in editor)

Wire these into the DiffViewer rendering in CenterPanel:
- When a diff tab is active, render a flex row: DiffFileList (left, 200px) | (right: DiffToolbar on top, DiffContextBar below it, DiffViewer filling remaining space)
- This creates the layout: file list sidebar | toolbar + context + diff
- The DiffFileList, DiffToolbar, and DiffContextBar wrap the DiffViewer -- create a DiffPanel.tsx wrapper component if needed, or compose directly in CenterPanel's diff rendering branch
  </action>
  <verify>
    Run: `cd /c/Users/USER/locus/frontend && npx tsc --noEmit 2>&1 | head -20` -- no errors.
    Open a diff: should see file list on left, toolbar with split/unified toggle on top, context bar with breadcrumb, and the diff viewer filling the rest.
  </verify>
  <done>DiffFileList shows changed files with status icons. DiffToolbar has split/unified toggle (working) and placeholder review buttons. DiffContextBar shows repo/branch/file breadcrumb for local diffs. Full diff layout is assembled: file list | (toolbar + context + diff viewer).</done>
</task>

</tasks>

<verification>
- Diff view renders with @git-diff-view/react (syntax highlighting visible, split view)
- Toggle between split and unified works, preference saved to localStorage
- File list sidebar shows changed files with status icons
- Clicking different files in the file list switches which diff is shown
- Context bar shows repo > branch > file breadcrumb
- Toolbar shows split/unified toggle and placeholder review buttons
- Large diffs scroll smoothly (virtual scrolling)
</verification>

<success_criteria>
Phase 2's manual DiffLine renderer is completely replaced with @git-diff-view/react. User sees split view by default with syntax highlighting and virtual scrolling. Changed file list sidebar allows switching between files. Toolbar has working view mode toggle and placeholder review buttons. Context bar shows appropriate breadcrumb.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-review-diff-editing/03-04-SUMMARY.md`
</output>
