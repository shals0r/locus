---
phase: 03-code-review-diff-editing
plan: 09
type: execute
wave: 4
depends_on: ["03-04", "03-07"]
files_modified:
  - frontend/src/components/filetree/FileSearch.tsx
  - frontend/src/components/editor/FileBreadcrumb.tsx
  - frontend/src/components/navigation/SidebarTabs.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/palette/CommandPalette.tsx
  - frontend/src/stores/commandPaletteStore.ts
autonomous: true

must_haves:
  truths:
    - "Cross-file search in sidebar Search tab shows results linking to editor tabs"
    - "Clickable breadcrumb navigation at top of editor/diff tabs (repo > branch > path > file)"
    - "Command palette extended with file/editor actions: Open file, Go to line, Search in files"
    - "Opening a file from a diff view opens a separate editor tab"
  artifacts:
    - path: "frontend/src/components/filetree/FileSearch.tsx"
      provides: "Cross-file search panel for sidebar"
      min_lines: 40
    - path: "frontend/src/components/editor/FileBreadcrumb.tsx"
      provides: "Breadcrumb navigation for editor/diff tabs"
      min_lines: 25
  key_links:
    - from: "frontend/src/components/filetree/FileSearch.tsx"
      to: "frontend/src/stores/sessionStore.ts"
      via: "openEditorTab on result click"
      pattern: "openEditorTab"
    - from: "frontend/src/components/editor/FileBreadcrumb.tsx"
      to: "frontend/src/stores/repoStore.ts"
      via: "repo/branch context"
      pattern: "useRepoStore"
---

<objective>
Add cross-file search, breadcrumb navigation, command palette extensions, and diff-to-editor file opening.

Purpose: These features complete the navigation and discovery experience. Search lets users find code across files. Breadcrumbs provide context and navigation. Command palette extensions bring file/editor actions into the keyboard-first workflow. Diff-to-editor opening connects the review and editing surfaces.

Output: FileSearch in sidebar Search tab, FileBreadcrumb above editor/diff content, command palette with file actions, "Open in Editor" from diff view.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-review-diff-editing/03-04-SUMMARY.md
@.planning/phases/03-code-review-diff-editing/03-07-SUMMARY.md
@frontend/src/components/palette/CommandPalette.tsx
@frontend/src/stores/commandPaletteStore.ts
@frontend/src/stores/sessionStore.ts
@frontend/src/stores/repoStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: FileSearch component + sidebar Search tab wiring</name>
  <files>
    frontend/src/components/filetree/FileSearch.tsx
    frontend/src/components/navigation/SidebarTabs.tsx
    frontend/src/components/layout/Sidebar.tsx
    backend/app/api/files.py
  </files>
  <action>
Add to backend/app/api/files.py:

`GET /api/files/search?machine_id=&repo_path=&query=&max_results=50` - Search for text within files in a repo. Use `grep -rn --include='*.{common extensions}' -l {query} {repo_path}` on the machine via run_command_on_machine. Return list of `{ file: str, matches: list[{ line: int, text: str }] }`. Limit to first 50 matching files and first 5 matches per file. Use `grep -rn` for line numbers and context. Exclude common directories (.git, node_modules, __pycache__, .venv, dist, build) via --exclude-dir.

Create frontend/src/components/filetree/FileSearch.tsx:
- Search panel for the sidebar "Search" tab
- Input field at top: search query text input with Search icon, debounced (300ms)
- Below input: search options row: "Match Case" toggle (Aa icon), "Regex" toggle (.* icon)
- Results area: scrollable list of matching files
  - Each file entry: file icon, relative path, match count badge
  - Expandable: click to show matched lines with line numbers
  - Each matched line: clickable -- opens the file in editor at that line number (use openEditorTab + store line number to scroll to on editor open)
- Loading spinner while search is in progress
- Empty state: "Type to search across files in this repository"
- No results state: "No matches found for '{query}'"
- Uses the selected repo from repoStore (selectedMachineId, selectedRepoPath)

Wire into Sidebar:
- Update Sidebar.tsx: when SidebarTabs active tab is "Search", render FileSearch instead of CommitTimeline+ChangedFiles
- Remove any remaining placeholder text from SidebarTabs for the Search tab
  </action>
  <verify>
    Run: `cd /c/Users/USER/locus/frontend && npx tsc --noEmit 2>&1 | head -20` -- no errors.
    Switch to Search tab in sidebar, type a query -- should see matching files with line-level results.
  </verify>
  <done>FileSearch component provides cross-file text search within the selected repo. Results show file paths with expandable matched lines. Clicking a match opens the file in an editor tab. Backend search uses grep via machine_registry for local/remote support.</done>
</task>

<task type="auto">
  <name>Task 2: FileBreadcrumb + command palette extensions + diff-to-editor</name>
  <files>
    frontend/src/components/editor/FileBreadcrumb.tsx
    frontend/src/components/palette/CommandPalette.tsx
    frontend/src/stores/commandPaletteStore.ts
    frontend/src/components/diff/DiffToolbar.tsx
  </files>
  <action>
Create frontend/src/components/editor/FileBreadcrumb.tsx:
- Horizontal breadcrumb bar shown at the top of editor and diff tab content
- Segments: repo name > branch > path segments > file name
- Each segment is clickable (future: navigate to that level in file tree). For now, clicking repo name switches to sidebar Git tab, clicking a path segment switches to sidebar Files tab at that directory.
- Separator: ChevronRight icon (text-muted, size 12)
- Styling: h-7, bg-dominant, text-xs, flex items-center, px-3
- For editor tabs: derive from editorData (machineId, repoPath, filePath) + repoStore (branch info)
- For diff tabs: derive from diffData (repoPath, filePath)
- Integration: render FileBreadcrumb in CenterPanel above the editor/diff content area when the active tab is type "editor" or "diff"

Extend frontend/src/components/palette/CommandPalette.tsx:
- Add new command groups for file/editor actions:
  - "Open File..." (type: action) - switches to sidebar Files tab and focuses the file tree. If text entered, it becomes a file name search filter.
  - "Go to Line..." (type: action) - if an editor tab is active, prompt for line number, then scroll editor to that line using editorRef.current?.revealLineInCenter(lineNumber)
  - "Search in Files..." (type: action) - switches to sidebar Search tab and focuses the search input
  - "Toggle Split/Unified Diff" (type: action) - if a diff tab is active, toggle the diff view mode
  - "Save File" (type: action) - if an editor tab is active, trigger save
- Register these in commandPaletteStore or wherever command registration happens

Add "Open in Editor" to DiffToolbar:
- New button in the diff toolbar (visible for file diffs, not just commit-level diffs)
- FileEdit icon + "Edit" text
- On click: open the current diff file in a new editor tab via sessionStore.openEditorTab(machineId, repoPath, filePath)
- This satisfies the locked decision: "Opening a file from a diff view opens a separate editor tab (diff and editor are independent tabs)"
  </action>
  <verify>
    Run: `cd /c/Users/USER/locus/frontend && npx tsc --noEmit 2>&1 | head -20` -- no errors.
    Editor/diff tabs show breadcrumb navigation. Command palette shows new file/editor commands. "Edit" button in diff toolbar opens editor tab for that file.
  </verify>
  <done>FileBreadcrumb shows repo > branch > path > file with clickable segments. Command palette extended with Open File, Go to Line, Search in Files, Toggle Diff Mode, Save File. Diff toolbar "Edit" button opens the file in a separate editor tab.</done>
</task>

</tasks>

<verification>
- Search tab in sidebar: type query, see file matches with line numbers
- Click search result: opens editor tab at that file
- Breadcrumb visible above editor and diff content
- Command palette: Ctrl+K shows file/editor commands
- "Edit" button in diff toolbar opens editor tab for the file
- Editor tab and diff tab are independent (both can be open for same file)
</verification>

<success_criteria>
Cross-file search works in sidebar Search tab with file and line-level results. Breadcrumb navigation shows above editor/diff tabs. Command palette has file/editor actions. Opening a file from diff view creates a separate editor tab.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-review-diff-editing/03-09-SUMMARY.md`
</output>
