---
phase: 03-code-review-diff-editing
plan: 10
type: execute
wave: 5
depends_on: ["03-05", "03-06", "03-07", "03-08", "03-09"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All Phase 3 success criteria are visually and functionally verified"
    - "Diff viewer shows split view with syntax highlighting"
    - "AI review produces annotations visible in diff"
    - "Monaco editor opens files with dark theme and save works"
    - "File tree shows directory structure"
    - "MR/PR task card actions work end-to-end (with credentials)"
---

<objective>
Visual and functional verification of all Phase 3 capabilities.

Purpose: Confirm that the diff viewer, AI review, code editor, file tree, review chat, and cross-feature integration all work correctly from a user perspective before marking the phase complete.

Output: Verified phase with user approval.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 3: Code Review, Diff, and Editing surface including:
  - Diff viewer with split/unified views, syntax highlighting, virtual scrolling, file list sidebar
  - AI review with Claude annotations, gutter icons, annotation panel, batch post
  - MR/PR comment display and thread replies
  - Monaco code editor with dark theme, minimap, Ctrl+S save, unsaved indicators
  - File tree browser with CRUD operations
  - Cross-file search
  - Breadcrumb navigation
  - Command palette extensions
  - Review contextual chat
  - MR/PR task card actions on the board
  </what-built>
  <how-to-verify>
**First, run these automated checks before visual verification:**

1. TypeScript compilation: `cd frontend && npx tsc --noEmit` -- must pass
2. Backend import check: verify all new modules import without error:
   ```python
   from app.api.files import router as files_router
   from app.api.review import router as review_router
   from app.api.ai_review import router as ai_review_router
   from app.services.file_service import read_file, write_file
   from app.services.review_service import get_review_provider
   from app.services.ai_review_service import review_diff
   ```
3. Frontend build: `cd frontend && npm run build` -- must succeed without errors
4. Check all new component files exist and are non-empty:
   - frontend/src/components/diff/DiffViewer.tsx (replaced)
   - frontend/src/components/diff/DiffFileList.tsx
   - frontend/src/components/diff/DiffToolbar.tsx
   - frontend/src/components/diff/DiffContextBar.tsx
   - frontend/src/components/diff/MrMetadataHeader.tsx
   - frontend/src/components/diff/CommentThread.tsx
   - frontend/src/components/diff/AnnotationPanel.tsx
   - frontend/src/components/diff/AnnotationGutter.tsx
   - frontend/src/components/diff/ReviewSubmitDialog.tsx
   - frontend/src/components/editor/CodeEditor.tsx
   - frontend/src/components/editor/FileBreadcrumb.tsx
   - frontend/src/components/editor/UnsavedDialog.tsx
   - frontend/src/components/filetree/FileTree.tsx
   - frontend/src/components/filetree/FileTreeNode.tsx
   - frontend/src/components/filetree/FileSearch.tsx
   - frontend/src/components/review/ReviewChat.tsx
   - frontend/src/components/review/ReviewAnnotations.tsx
   - frontend/src/components/navigation/SidebarTabs.tsx
5. Run backend tests if they exist: `cd backend && python -m pytest -x --tb=short 2>&1 | tail -20`
6. Run frontend tests if they exist: `cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -20`

**Then verify visually and functionally:**

1. **Diff Viewer (DIFF-01):** Navigate to a repo with uncommitted changes. Click a changed file in the git sidebar. Verify:
   - Diff opens in a tab with split (side-by-side) view
   - Syntax highlighting visible on both sides
   - Toggle to unified view, toggle back -- preference should persist on page refresh
   - File list sidebar on the left shows changed files with status icons
   - Click different files to switch between diffs
   - Large diff scrolls smoothly (virtual scrolling)

2. **Code Editor (EDIT-01, EDIT-02, EDIT-03):** Switch to Files tab in sidebar.
   - File tree shows the repository directory structure
   - Click a file to open in Monaco editor
   - Verify: dark theme, minimap on right, syntax highlighting
   - Edit some text -- tab should show dot (unsaved indicator)
   - Press Ctrl+S -- file should save (dot disappears)
   - Open a second file -- verify multiple editor tabs
   - Close a tab with unsaved changes -- verify Save/Discard/Cancel dialog
   - Right-click in file tree: try creating a new file, renaming, deleting
   - Click "Edit" button in a diff toolbar -- verify separate editor tab opens for that file

3. **Breadcrumb + Search + Palette:**
   - Breadcrumb visible above editor/diff tabs showing repo > branch > path
   - Switch to Search tab in sidebar, search for a term -- verify results
   - Open command palette (Ctrl+K), type "open" -- verify file commands appear

4. **AI Review (DIFF-03):** (requires LOCUS_LLM_API_KEY configured)
   - In a diff view, click "Review with Claude"
   - Optionally enter custom instructions
   - Wait for review (loading spinner)
   - Verify annotations appear as colored gutter icons
   - Click a gutter icon -- annotation panel opens from right
   - Edit an annotation's text in the panel
   - Note: posting as MR/PR comments requires GitHub/GitLab credentials

5. **MR/PR Actions (DIFF-02, DIFF-04, DIFF-05):** (requires GitHub/GitLab integration configured)
   - If you have a task card linked to an MR/PR, verify it shows action buttons
   - Click "View Diff" -- should open MR/PR diff with metadata header
   - Existing teammate comments should appear inline
   - Verify reply to thread works

6. **Review Chat:** Click the chat toggle (in diff toolbar or annotation panel) -- verify chat panel slides in from right edge, resizable.

  </how-to-verify>
  <resume-signal>Type "approved" if Phase 3 meets expectations, or describe any issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
All Phase 3 success criteria verified:
1. User can view diffs of local changes in unified or split diff view
2. User can view MR/PR diffs from GitLab and GitHub in same viewer
3. User can trigger AI review and see annotations inline, promote to comments
4. User can approve or request changes on MRs/PRs
5. User can browse file tree, open files in Monaco, edit, save back
6. User can open file from diff to edit, see diff update after saving
</verification>

<success_criteria>
User approves all six Phase 3 success criteria. TypeScript compiles, backend imports succeed, frontend builds.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-review-diff-editing/03-10-SUMMARY.md`
</output>
