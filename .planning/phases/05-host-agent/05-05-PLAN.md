---
phase: 05-host-agent
plan: 05
type: execute
wave: 3
depends_on:
  - "05-04"
files_modified:
  - backend/app/services/machine_registry.py
  - backend/app/local/manager.py
  - backend/app/ws/terminal.py
  - backend/app/services/claude.py
  - backend/app/config.py
  - docker-compose.yml
  - backend/app/agent/exec.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-05
  - AGENT-06

must_haves:
  truths:
    - "Local machine routes through agent API when LOCUS_AGENT_URL is set, not SSH"
    - "Terminal WebSocket proxies to agent instead of creating SSH channels"
    - "Claude detection uses agent endpoint instead of SSH exec"
    - "Docker Compose mounts agent volume and sets LOCUS_AGENT_URL environment variable"
    - "Machine registry dispatches to agent client as third transport"
    - "needs_setup shown with clear instructions when agent is not running"
  artifacts:
    - path: "backend/app/services/machine_registry.py"
      provides: "Updated registry routing through agent client"
    - path: "backend/app/local/manager.py"
      provides: "Updated local manager probing agent first"
    - path: "backend/app/ws/terminal.py"
      provides: "Updated terminal WS routing through agent proxy"
    - path: "docker-compose.yml"
      provides: "Agent volume mount and LOCUS_AGENT_URL env var"
    - path: "backend/app/agent/exec.py"
      provides: "Command execution via agent REST API"
      exports: ["run_command_via_agent"]
  key_links:
    - from: "backend/app/services/machine_registry.py"
      to: "backend/app/agent/client.py"
      via: "AgentClient for local machine operations"
      pattern: "agent_client"
    - from: "backend/app/ws/terminal.py"
      to: "backend/app/agent/proxy.py"
      via: "proxy_terminal_to_agent for agent-backed sessions"
      pattern: "proxy_terminal_to_agent"
    - from: "backend/app/local/manager.py"
      to: "backend/app/agent/deployer.py"
      via: "probe_agent on startup"
      pattern: "probe_agent"
---

<objective>
Rewire the Locus backend to route local machine operations (terminals, commands, Claude detection) through the host agent instead of SSH, and update Docker Compose for agent support.

Purpose: This is the integration layer that connects the existing Locus backend to the new agent. After this plan, "This Machine" works through the agent rather than SSH-to-localhost.
Output: Updated machine_registry, local/manager, ws/terminal, claude service, and docker-compose.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-host-agent/05-CONTEXT.md
@.planning/phases/05-host-agent/05-RESEARCH.md

<interfaces>
<!-- AgentClient from Plan 04 -->
From backend/app/agent/client.py:
```python
class AgentClient:
    def __init__(self, base_url: str, token: str)
    async def health(self) -> dict
    async def create_terminal(self, cols, rows, working_dir=None, tmux_session=None) -> dict
    async def close_terminal(self, session_id: str) -> None
    async def list_tmux_sessions(self) -> list[dict]
    async def detect_claude_sessions(self) -> list[dict]
    async def run_command(self, command: str) -> str
    def terminal_ws_url(self, session_id: str) -> str
    async def close(self)
```

From backend/app/agent/deployer.py:
```python
async def probe_agent(base_url: str, token: str | None = None) -> dict | None
async def ensure_agent(ssh_conn, host, port=7700, expected_version=None) -> tuple[str, str]
```

From backend/app/agent/proxy.py:
```python
async def proxy_terminal_to_agent(browser_ws, agent_ws_url: str) -> None
```

<!-- Current interfaces being modified -->
From backend/app/services/machine_registry.py:
```python
async def get_connection_for_machine(machine_id: str)
def get_machine_status(machine_id: str) -> str
async def run_command_on_machine(machine_id: str, command: str) -> str
```

From backend/app/local/manager.py:
```python
class LocalMachineManager:
    async def initialize(self)
    async def get_connection(self)
    is_usable -> bool
    async def run_command(self, command: str) -> str
    def get_status(self) -> str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update local/manager.py to probe agent and config.py for agent settings</name>
  <files>
    backend/app/local/manager.py,
    backend/app/config.py,
    backend/app/agent/exec.py
  </files>
  <read_first>
    backend/app/local/manager.py (current LocalMachineManager — being modified),
    backend/app/config.py (current settings — adding agent fields),
    backend/app/agent/client.py (AgentClient from Plan 04),
    backend/app/agent/deployer.py (probe_agent from Plan 04)
  </read_first>
  <action>
    **backend/app/config.py** — Add agent settings (per D-06, D-20):
    - Add to Settings class:
      ```python
      agent_url: str = ""  # env var LOCUS_AGENT_URL (e.g., http://host.docker.internal:7700)
      agent_token_file: str = ""  # env var LOCUS_AGENT_TOKEN_FILE (e.g., /opt/locus-agent/agent.token)
      ```
    - These are optional — if empty, agent is not used (falls back to SSH path).

    **backend/app/agent/exec.py** — Simple command execution wrapper:
    - `async def run_command_via_agent(agent_client: AgentClient, command: str) -> str`:
      - POST to agent's /exec endpoint (or use agent_client.run_command)
      - Return stdout string
    - Note: The agent needs a `/exec` endpoint too. Add a note in the code that Plan 03's app.py update should include an exec endpoint, OR add a simple exec endpoint to the agent API here as a TODO.
    - Actually, add the exec endpoint to the agent: create `agent/locus_agent/api/exec.py` with `POST /exec` that runs commands via `asyncio.create_subprocess_shell` and returns `{"stdout": ..., "stderr": ..., "returncode": ...}`. Wire into app.py.

    **backend/app/local/manager.py** — Update to probe agent first (per D-06, D-20):
    - Add `_agent_client: AgentClient | None = None` field to LocalMachineManager
    - Add `_agent_available: bool = False` field
    - Update `async def initialize(self)`:
      - If `settings.agent_url` is set:
        1. Read token from `settings.agent_token_file` if file exists
        2. Create `AgentClient(settings.agent_url, token)`
        3. Try `probe_agent(settings.agent_url, token)`
        4. If probe succeeds: set `_agent_client`, `_agent_available = True`, log "Local machine: connected via host agent"
        5. If probe fails: log warning "Host agent not reachable at {url} — falling back to SSH"
      - Then fall through to existing SSH path if agent not available
    - Update `is_usable` property:
      - If `_agent_available`: return True
      - Else: existing logic (native mode or Docker+SSH)
    - Update `get_status()`:
      - If `_agent_available`: return "online"
      - Else: existing logic
    - Update `async def run_command(self, command: str) -> str`:
      - If `_agent_client`: use `self._agent_client.run_command(command)` — return stdout
      - Else: existing logic (SSH or subprocess)
    - Add `@property agent_client(self) -> AgentClient | None`: Return `_agent_client`
    - Update `async def shutdown(self)`:
      - If `_agent_client`: `await self._agent_client.close()`
      - Then existing SSH shutdown
    - Keep SSH fallback intact — agent is preferred but not required.
  </action>
  <verify>
    <automated>cd /home/samy/locus && grep -c "agent_url" backend/app/config.py && grep -c "_agent_client" backend/app/local/manager.py && grep -c "agent_client" backend/app/local/manager.py && python -c "from backend.app.agent.exec import run_command_via_agent; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/config.py contains `agent_url: str = ""` and `agent_token_file: str = ""`
    - backend/app/local/manager.py contains `_agent_client: AgentClient | None = None`
    - backend/app/local/manager.py initialize() probes agent_url if set
    - backend/app/local/manager.py run_command() prefers agent_client over SSH/subprocess
    - backend/app/local/manager.py is_usable returns True when agent is available
    - backend/app/local/manager.py shutdown() closes agent_client
    - agent/locus_agent/api/exec.py contains `POST /exec` endpoint running commands via subprocess
    - SSH fallback preserved when agent is not available
  </acceptance_criteria>
  <done>LocalMachineManager probes agent on startup, uses agent for commands when available, falls back to SSH, config has agent_url/agent_token_file settings</done>
</task>

<task type="auto">
  <name>Task 2: Rewire machine_registry, ws/terminal, claude service, and docker-compose</name>
  <files>
    backend/app/services/machine_registry.py,
    backend/app/ws/terminal.py,
    backend/app/services/claude.py,
    docker-compose.yml
  </files>
  <read_first>
    backend/app/services/machine_registry.py (current routing logic — being modified),
    backend/app/ws/terminal.py (current terminal WS handler — adding agent proxy path),
    backend/app/services/claude.py (current Claude detection — adding agent path),
    docker-compose.yml (current Docker config — adding agent env vars and volume),
    backend/app/agent/client.py (AgentClient from Plan 04),
    backend/app/agent/proxy.py (proxy_terminal_to_agent from Plan 04),
    backend/app/local/manager.py (updated in Task 1 — agent_client property)
  </read_first>
  <action>
    **backend/app/services/machine_registry.py** — Add agent routing:
    - Update `get_connection_for_machine(machine_id)`:
      - For local machine: if `local_machine_manager.agent_client` is not None, return a sentinel value (e.g., `"agent"` string) instead of SSH connection. Callers need to check for this.
      - Actually, better approach: Add a new function `get_agent_client_for_machine(machine_id: str) -> AgentClient | None` that returns the agent client if available for that machine.
      - Keep `get_connection_for_machine` returning SSH connections as before for backward compat.
    - Add `async def get_agent_client_for_machine(machine_id: str) -> AgentClient | None`:
      - If local machine and `local_machine_manager.agent_client`: return it
      - For remote machines: future support (return None for now — remote agent support comes later)
      - Return None otherwise
    - Update `run_command_on_machine(machine_id, command)`:
      - If local machine: delegate to `local_machine_manager.run_command(command)` (which already prefers agent)
      - Remote: existing SSH path

    **backend/app/ws/terminal.py** — Add agent proxy path:
    - Import `get_agent_client_for_machine` from machine_registry
    - Import `proxy_terminal_to_agent` from agent.proxy
    - In `_get_or_create_process()`:
      - Before the existing SSH/local logic, check: `agent_client = await get_agent_client_for_machine(machine_id)`
      - If agent_client is not None:
        - Call `agent_client.create_terminal(cols, rows, working_dir=terminal_session.repo_path, tmux_session=terminal_session.tmux_session_name)`
        - Get back `{"session_id": agent_sid, "tmux_name": ...}`
        - Store agent session mapping for proxy lookup
        - Return a special `AgentSessionProcess` that holds agent_client + agent_session_id
      - Add class `AgentSessionProcess` in the same file:
        - Implements same interface as SessionProcess (attach, detach, get_scrollback, write, resize, close, alive)
        - But instead of direct PTY I/O, it holds the agent_ws_url and delegates to the proxy
        - Actually, simpler: Don't use AgentSessionProcess. Instead, modify the `terminal_websocket` handler directly:
          - After session lookup, check if agent is available for this machine
          - If yes: create terminal on agent, then call `proxy_terminal_to_agent(websocket, agent_client.terminal_ws_url(agent_sid))`
          - If no: existing SSH/local path
    - Update `terminal_websocket()`:
      - After accepting and looking up terminal_session, check `agent_client = await get_agent_client_for_machine(machine_id)`
      - If agent_client:
        - Create terminal on agent: `result = await agent_client.create_terminal(cols, rows, working_dir=terminal_session.repo_path, tmux_session=terminal_session.tmux_session_name)`
        - Save tmux_name to DB if new
        - Call `await proxy_terminal_to_agent(websocket, agent_client.terminal_ws_url(result["session_id"]))`
        - Return (skip existing SSH path)
      - Else: existing _get_or_create_process flow

    **backend/app/services/claude.py** — Add agent path:
    - Import `local_machine_manager` and check for agent_client
    - Add `async def detect_claude_sessions_via_agent(agent_client: AgentClient) -> list[dict]`:
      - Call `agent_client.detect_claude_sessions()`
      - Return the response list (same format as SSH-based detection)
    - In existing detect functions (or wherever called from status WS), check if agent available and use agent path.
    - Add a convenience function: `async def detect_claude_sessions_for_machine(machine_id: str) -> list[dict]`:
      - If local machine with agent: use agent client
      - If local machine without agent but usable: use existing subprocess detection
      - If remote machine: use existing SSH detection
      - Return unified format

    **docker-compose.yml** (per D-06, D-20):
    - Add to `app` service environment:
      ```yaml
      LOCUS_AGENT_URL: "http://host.docker.internal:7700"
      LOCUS_AGENT_TOKEN_FILE: "/opt/locus-agent/agent.token"
      ```
    - Add volume mount to `app` service:
      ```yaml
      - agent-data:/opt/locus-agent:ro
      ```
    - Add to volumes section:
      ```yaml
      agent-data:
        driver: local
        driver_opts:
          type: none
          o: bind
          device: ${LOCUS_AGENT_DIR:-${HOME}/.locus-agent}
      ```
    - Add same environment and volume to `app-dev` service
    - NOTE: Keep existing `extra_hosts: ["host.docker.internal:host-gateway"]` (per Pitfall 5, already present)
  </action>
  <verify>
    <automated>cd /home/samy/locus && grep "LOCUS_AGENT_URL" docker-compose.yml && grep "agent-data" docker-compose.yml && grep "get_agent_client_for_machine" backend/app/services/machine_registry.py && grep "proxy_terminal_to_agent" backend/app/ws/terminal.py && grep "detect_claude_sessions_for_machine\|detect_claude_sessions_via_agent" backend/app/services/claude.py && echo "ALL OK"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/machine_registry.py contains `async def get_agent_client_for_machine(` returning AgentClient or None
    - backend/app/ws/terminal.py imports and calls `proxy_terminal_to_agent` when agent is available
    - backend/app/ws/terminal.py creates terminal on agent before proxying
    - backend/app/services/claude.py contains agent-based Claude detection path
    - docker-compose.yml `app` service has `LOCUS_AGENT_URL: "http://host.docker.internal:7700"`
    - docker-compose.yml `app` service has `LOCUS_AGENT_TOKEN_FILE: "/opt/locus-agent/agent.token"`
    - docker-compose.yml has `agent-data` volume with bind mount to `~/.locus-agent`
    - docker-compose.yml `app-dev` service has same agent config
    - Existing SSH fallback still works when agent is not available
  </acceptance_criteria>
  <done>Full integration: machine_registry routes through agent, terminal WS proxies to agent, Claude detection uses agent, Docker Compose configured for agent discovery</done>
</task>

</tasks>

<verification>
- `grep -c "agent" backend/app/services/machine_registry.py` shows agent routing added
- `grep "LOCUS_AGENT" docker-compose.yml` shows both URL and token file configured
- `grep "proxy_terminal_to_agent" backend/app/ws/terminal.py` confirms proxy wiring
- Existing SSH paths still present as fallback (grep for ssh_manager in ws/terminal.py)
</verification>

<success_criteria>
- Local machine uses agent when available, SSH when not
- Terminal WebSocket proxied through agent for agent-backed machines
- Claude detection works through agent endpoint
- Docker Compose configured with agent volume and environment variables
- Zero regression — SSH path still works for machines without agents
</success_criteria>

<output>
After completion, create `.planning/phases/05-host-agent/05-05-SUMMARY.md`
</output>
