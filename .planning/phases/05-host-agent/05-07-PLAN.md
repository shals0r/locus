---
phase: 05-host-agent
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/CenterPanel.tsx
autonomous: true
gap_closure: true
requirements:
  - AGENT-01
  - AGENT-06

must_haves:
  truths:
    - "When agent is not running, 'This Machine' needs_setup panel shows the exact locus-agent start command"
    - "No reference to 'Coming in Phase 5' or any future-tense placeholder text exists in the needs_setup panel"
    - "Option A (Locus Host Agent) is presented as the primary recommended setup path with actionable instructions"
  artifacts:
    - path: "frontend/src/components/layout/CenterPanel.tsx"
      provides: "needs_setup panel with real agent start instructions"
      contains: "locus-agent start"
  key_links:
    - from: "frontend/src/components/layout/CenterPanel.tsx"
      to: "agent/locus_agent/cli.py"
      via: "UI text references the CLI command locus-agent start"
      pattern: "locus-agent start"
---

<objective>
Replace stale "Coming in Phase 5" placeholder text in the CenterPanel needs_setup panel with actual locus-agent start instructions.

Purpose: The host agent is fully built (plans 05-01 through 05-06 complete), but the frontend still tells the user the agent is "Coming in Phase 5." This blocks Success Criteria 1 (partial — UI instructions wrong) and Success Criteria 6 ("clear instructions" not clear). Users who see "This Machine" in needs_setup state get no actionable guidance.

Output: Updated CenterPanel.tsx with real setup instructions showing how to install and start the locus-agent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-host-agent/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace needs_setup placeholder with actual agent start instructions</name>
  <files>frontend/src/components/layout/CenterPanel.tsx</files>
  <read_first>
    - frontend/src/components/layout/CenterPanel.tsx (the file being modified — see lines 128-163 for the needs_setup panel)
    - agent/pyproject.toml (confirms the CLI entry point name: locus-agent)
    - agent/locus_agent/config.py (confirms default port 7700 and LOCUS_AGENT_ env prefix)
  </read_first>
  <action>
In CenterPanel.tsx, replace the Option A block (lines 140-148) which currently reads:

```
<div className="rounded border border-border bg-secondary p-3">
  <p className="text-sm font-medium text-primary-text">
    Option A: Locus Host Agent
  </p>
  <p className="mt-1 text-xs text-muted">
    A lightweight process running on your host that bridges Docker
    to the host. Coming in Phase 5.
  </p>
</div>
```

Replace with a block that:

1. Changes the heading to "Option A: Locus Host Agent (Recommended)" — add "(Recommended)" to signal this is the primary path
2. Replaces the description paragraph with TWO elements:
   - A brief description: "A lightweight process running on your host that bridges Docker to the host."
   - A code block showing the install+start commands using a `<pre>` with `<code>` inside, styled with `className="mt-2 rounded bg-dominant px-3 py-2 text-xs text-accent font-mono whitespace-pre"`. The commands to show:
     ```
     pip install ./agent
     locus-agent start
     ```
3. Adds a note below the code block: `<p className="mt-1 text-xs text-muted">Runs on port 7700 by default. Locus auto-detects the agent from Docker.</p>`

Keep Option B (SSH to host) block exactly as-is — it remains a valid fallback.

Do NOT change anything outside lines 128-163 (the machineNeedsSetup conditional block). The rest of CenterPanel.tsx must remain untouched.
  </action>
  <verify>
    <automated>cd /home/samy/locus && grep -n "Coming in Phase 5" frontend/src/components/layout/CenterPanel.tsx; echo "EXIT:$?"; grep -n "locus-agent start" frontend/src/components/layout/CenterPanel.tsx; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Coming in Phase 5" returns NO matches (exit code 1) — stale text fully removed
    - grep "locus-agent start" returns at least 1 match (exit code 0) — actual command is present
    - grep "pip install ./agent" returns at least 1 match — install command is present
    - grep "Recommended" returns at least 1 match in the Option A heading
    - grep "port 7700" returns at least 1 match — port info shown to user
    - grep "auto-detects" returns at least 1 match — user told detection is automatic
    - The file still contains "Option B: SSH to host" unchanged
    - No TypeScript compilation errors: cd frontend && npx tsc --noEmit passes (or at minimum the file parses without syntax errors)
  </acceptance_criteria>
  <done>CenterPanel.tsx needs_setup panel shows "pip install ./agent" and "locus-agent start" commands with port info, no "Coming in Phase 5" text remains, Option B SSH fallback unchanged</done>
</task>

</tasks>

<verification>
1. `grep -c "Coming in Phase 5" frontend/src/components/layout/CenterPanel.tsx` returns 0
2. `grep -c "locus-agent start" frontend/src/components/layout/CenterPanel.tsx` returns at least 1
3. `grep -c "pip install" frontend/src/components/layout/CenterPanel.tsx` returns at least 1
4. `grep "Option B: SSH to host" frontend/src/components/layout/CenterPanel.tsx` still present
5. No new lint or TypeScript errors introduced
</verification>

<success_criteria>
- The needs_setup panel for "This Machine" shows the actual locus-agent installation and start commands
- No stale "Coming in Phase 5" text remains anywhere in the file
- Option B (SSH fallback) instructions remain unchanged
- Success Criteria 1 and 6 from the phase are now fully satisfiable (pending human visual verification of the running UI)
</success_criteria>

<output>
After completion, create `.planning/phases/05-host-agent/05-07-SUMMARY.md`
</output>
