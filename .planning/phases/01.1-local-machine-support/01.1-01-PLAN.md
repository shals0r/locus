---
phase: 01.1-local-machine-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/local/__init__.py
  - backend/app/local/manager.py
  - backend/app/local/tmux.py
  - backend/app/local/terminal.py
  - backend/app/services/machine_registry.py
  - backend/app/config.py
  - backend/app/models/session.py
  - docker-compose.yml
autonomous: true

must_haves:
  truths:
    - "LocalMachineManager auto-detects Docker vs native mode"
    - "Machine registry returns connection for any machine_id (local or remote)"
    - "TerminalSession can be created with machine_id='local' without FK violation"
    - "Docker containers can resolve host.docker.internal"
    - "App starts without error when terminal_sessions table has old UUID schema (auto-migrated on startup)"
  artifacts:
    - path: "backend/app/local/__init__.py"
      provides: "Module exports for local machine support"
    - path: "backend/app/local/manager.py"
      provides: "LocalMachineManager class + LOCAL_MACHINE_ID constant"
      exports: ["LocalMachineManager", "local_machine_manager", "LOCAL_MACHINE_ID", "LOCAL_MACHINE_NAME"]
    - path: "backend/app/local/tmux.py"
      provides: "Local tmux operations via subprocess (native mode)"
      exports: ["list_tmux_sessions_local", "check_tmux_session_exists_local", "create_local_terminal_in_tmux", "kill_tmux_session_local"]
    - path: "backend/app/local/terminal.py"
      provides: "Local PTY terminal process class (native mode)"
      exports: ["LocalTerminalProcess"]
    - path: "backend/app/services/machine_registry.py"
      provides: "Unified machine lookup across local and remote"
      exports: ["get_connection_for_machine", "get_machine_status", "is_local_machine", "run_command_on_machine"]
  key_links:
    - from: "backend/app/services/machine_registry.py"
      to: "backend/app/local/manager.py"
      via: "import local_machine_manager"
      pattern: "from app\\.local\\.manager import"
    - from: "backend/app/services/machine_registry.py"
      to: "backend/app/ssh/manager.py"
      via: "import ssh_manager"
      pattern: "from app\\.ssh\\.manager import"
    - from: "backend/app/local/manager.py"
      to: "backend/app/config.py"
      via: "settings for host SSH config"
      pattern: "settings\\.local_"
---

<objective>
Create the local machine backend module, machine registry, and infrastructure needed for "This Machine" support.

Purpose: This is the foundation layer for Phase 1.1. It creates the LocalMachineManager (which auto-detects Docker vs native mode), local tmux/PTY subprocess helpers, a unified machine registry, and the DB migration to allow local machine sessions. All subsequent plans depend on this module existing.

Output: New `backend/app/local/` module, `services/machine_registry.py`, updated config, updated docker-compose.yml, and relaxed TerminalSession FK constraint with automated schema migration.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-local-machine-support/01.1-RESEARCH.md
@backend/app/ssh/manager.py
@backend/app/ssh/tmux.py
@backend/app/ssh/terminal.py
@backend/app/config.py
@backend/app/models/session.py
@backend/app/database.py
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create local machine module and machine registry</name>
  <files>
    backend/app/local/__init__.py
    backend/app/local/manager.py
    backend/app/local/tmux.py
    backend/app/local/terminal.py
    backend/app/services/machine_registry.py
    backend/app/config.py
  </files>
  <action>
Create the `backend/app/local/` package with four files:

**`__init__.py`**: Export `LocalMachineManager`, `local_machine_manager`, `LOCAL_MACHINE_ID`, `LOCAL_MACHINE_NAME`.

**`manager.py`**: Create `LocalMachineManager` class following the pattern from RESEARCH.md Pattern 1.
- Constants: `LOCAL_MACHINE_ID = "local"`, `LOCAL_MACHINE_NAME = "This Machine"`
- `is_running_in_docker()` function: checks `os.path.exists("/.dockerenv")` or `LOCUS_IN_DOCKER` env var
- Class has `_in_docker` flag set in `__init__`, `_ssh_conn` for Docker mode, `_status` always "online"
- `async initialize()`: In Docker mode, SSH to `host.docker.internal` using `settings.local_ssh_host`, `settings.local_ssh_port`, `settings.local_ssh_user`, `settings.local_ssh_key`. In native mode, no-op. Log warnings if Docker SSH config is missing (don't crash).
- `async get_connection()`: Returns AsyncSSH connection in Docker mode, `None` in native mode (sentinel for subprocess path)
- `async run_command(command: str) -> str`: SSH run in Docker mode, `asyncio.create_subprocess_shell` in native mode
- `get_status() -> str`: Always returns "online"
- `async shutdown()`: Close SSH connection if open
- Module-level singleton: `local_machine_manager = LocalMachineManager()`

**`tmux.py`**: Local tmux operations via subprocess (for native mode). Mirror the function signatures from `ssh/tmux.py` but use `asyncio.create_subprocess_exec` instead of SSH connection:
- `async list_tmux_sessions_local() -> list[dict]`: Run `tmux ls -F '...'`, parse same format as SSH version. Return empty list if tmux not installed (`FileNotFoundError`) or no server running (non-zero exit).
- `async check_tmux_session_exists_local(session_name: str) -> bool`: Run `tmux has-session -t {name}`.
- `async create_local_terminal_in_tmux(session_name, working_dir, cols, rows) -> tuple[LocalTerminalProcess, str]`: If session_name provided, attach; else create new with generated name. Use `_generate_local_session_name` helper (subprocess-based git branch detection). Create the tmux session using a PTY-backed subprocess. Import and use `LocalTerminalProcess` from `terminal.py`.
- `async kill_tmux_session_local(session_name: str) -> bool`: Run `tmux kill-session -t {name}`.
- Helper `_sanitize_tmux_name(raw)` -- same logic as `ssh/tmux.py`.

**`terminal.py`**: Create `LocalTerminalProcess` class from RESEARCH.md Pattern 4.
- Must provide same interface as AsyncSSH process so `SessionProcess` in `ws/terminal.py` can use it interchangeably: `.stdout.read(n)`, `.stdin.write(data)`, `.channel.set_terminal_size(cols, rows)`, `.close()`, `.wait_closed()`
- **CRITICAL duck-typing requirement:** `SessionProcess.resize()` calls `self.process.channel.set_terminal_size(cols, rows)`. Therefore `LocalTerminalProcess` MUST have a `.channel` attribute that has a `set_terminal_size(cols, rows)` method. Implement this as `@property def channel(self): return self` on `LocalTerminalProcess`, and implement `set_terminal_size(self, cols, rows)` directly on the class (calling `_set_size` internally). This way `process.channel.set_terminal_size(cols, rows)` works.
- Uses `pty.openpty()` + `os.fork()` to create PTY pair
- `_set_size()` via `TIOCSWINSZ` ioctl
- Non-blocking I/O on master fd via `fcntl`
- Async `read(n)` using `asyncio.get_event_loop().add_reader()` on master fd
- Synchronous `write(data)` via `os.write()` to master fd
- Gate all `pty`/`fcntl`/`termios` imports behind `sys.platform != "win32"` check with a clear error message
- Set `TERM=xterm-256color` in the child environment

**`services/machine_registry.py`**: Unified lookup (RESEARCH.md Pattern 2):
- `async get_connection_for_machine(machine_id: str)`: Returns local_machine_manager.get_connection() if local, else ssh_manager.get_connection()
- `def get_machine_status(machine_id: str) -> str`: Returns local_machine_manager.get_status() if local, else ssh_manager.get_status()
- `def is_local_machine(machine_id: str) -> bool`: Returns `machine_id == LOCAL_MACHINE_ID`
- `async run_command_on_machine(machine_id: str, command: str) -> str`: Routes to local_machine_manager.run_command() or SSH conn.run() based on machine_id

**`config.py`**: Add local machine SSH settings to the Settings class:
- `local_ssh_host: str = "host.docker.internal"`
- `local_ssh_port: int = 22`
- `local_ssh_user: str = ""` (empty = not configured)
- `local_ssh_key: str = ""` (empty = not configured)
- `local_repo_scan_paths: list[str] = []` (default empty, configured via `LOCUS_LOCAL_REPO_SCAN_PATHS` -- comma-separated env var that Pydantic parses as JSON list or via custom validator)
- `in_docker: bool = False` (set via `LOCUS_IN_DOCKER` env var)
  </action>
  <verify>
    Run `cd C:/Users/USER/locus/backend && PYTHONPATH=. python -c "from app.local.manager import local_machine_manager, LOCAL_MACHINE_ID; from app.services.machine_registry import get_connection_for_machine, is_local_machine; print('imports ok')"`. Verify no import errors.
  </verify>
  <done>
    All five new files exist with correct exports. LocalMachineManager detects Docker vs native. Machine registry routes by machine_id. Config has local_* settings. No import errors when loading the modules. LocalTerminalProcess has a `.channel` property (returning self) with `set_terminal_size(cols, rows)` callable, satisfying the SessionProcess duck-typing contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Relax TerminalSession FK constraint with automated migration, update docker-compose.yml</name>
  <files>
    backend/app/models/session.py
    backend/app/main.py
    docker-compose.yml
  </files>
  <action>
**`models/session.py`**: Modify the `TerminalSession` model to remove the FK constraint on `machine_id`:
- Change `machine_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("machines.id"))` to `machine_id: Mapped[str] = mapped_column(String(255))` -- a plain string field, no FK.
- This allows `machine_id="local"` for the local machine and UUID strings for remote machines.
- Remove `ForeignKey` from the `from sqlalchemy import ...` line if no longer used by this file (keep other imports).
- Add `String` to the sqlalchemy imports if not already present.
- The existing `session_type`, `tmux_session_name`, `repo_path`, and `is_active` columns remain unchanged.

**`main.py`**: Add automated schema migration in the lifespan to handle the column type change. After the existing `Base.metadata.create_all` call, add a migration block:

```python
# Migrate terminal_sessions.machine_id from UUID to VARCHAR if needed
async with engine.begin() as mig_conn:
    result = await mig_conn.execute(text(
        "SELECT data_type FROM information_schema.columns "
        "WHERE table_name = 'terminal_sessions' AND column_name = 'machine_id'"
    ))
    row = result.first()
    if row and row[0] == 'uuid':
        logger.info("Migrating terminal_sessions.machine_id from UUID to VARCHAR(255)...")
        await mig_conn.execute(text(
            "ALTER TABLE terminal_sessions "
            "DROP CONSTRAINT IF EXISTS terminal_sessions_machine_id_fkey"
        ))
        await mig_conn.execute(text(
            "ALTER TABLE terminal_sessions "
            "ALTER COLUMN machine_id TYPE VARCHAR(255) USING machine_id::text"
        ))
        logger.info("Migration complete: terminal_sessions.machine_id is now VARCHAR(255)")
```

Add `from sqlalchemy import text` import if not already present. This runs on every startup but is a no-op when the column is already VARCHAR. Handles both fresh databases (create_all creates correct schema) and existing databases (ALTER TABLE migrates).

**`docker-compose.yml`**: Add local machine support infrastructure:
- Add `extra_hosts: ["host.docker.internal:host-gateway"]` to BOTH the `app` and `app-dev` services (ensures Linux Docker Engine compatibility per RESEARCH.md Pitfall 1).
- Add environment variables to both `app` and `app-dev`:
  - `LOCUS_IN_DOCKER: "1"`
  - `LOCUS_LOCAL_SSH_HOST: host.docker.internal`
  - `LOCUS_LOCAL_SSH_PORT: ${LOCUS_HOST_SSH_PORT:-22}`
  - `LOCUS_LOCAL_SSH_USER: ${LOCUS_HOST_SSH_USER:-}`
  - `LOCUS_LOCAL_SSH_KEY: /home/locus/.ssh/id_ed25519`
- Keep all existing configuration unchanged.
  </action>
  <verify>
    Verify `session.py` no longer imports `ForeignKey` and `machine_id` is `Mapped[str]` with `String(255)`. Verify `main.py` has the ALTER TABLE migration block using `information_schema.columns` check. Verify `docker-compose.yml` has `extra_hosts` and the new environment variables in both `app` and `app-dev` services.
  </verify>
  <done>
    TerminalSession.machine_id is a plain String(255) field with no FK constraint. main.py lifespan auto-migrates existing UUID columns to VARCHAR(255) on startup (no manual `docker compose down -v` needed). docker-compose.yml has host.docker.internal resolution and local machine SSH env vars for both services.
  </done>
</task>

</tasks>

<verification>
- All files in `backend/app/local/` exist and are importable
- `machine_registry.py` correctly routes local vs remote
- `TerminalSession` model has no FK on machine_id
- `main.py` has automated ALTER TABLE migration for existing databases
- `docker-compose.yml` has extra_hosts and LOCUS_IN_DOCKER env var
- `config.py` has local_ssh_* settings
- `LocalTerminalProcess.channel.set_terminal_size()` works (duck-typing for SessionProcess.resize)
</verification>

<success_criteria>
The local machine module, machine registry, DB model change with automated migration, and Docker config are in place. No existing functionality is broken. Existing databases with UUID machine_id columns are automatically migrated on startup. The foundation is ready for Plan 02 to wire these into the API routes and WebSocket handlers.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-local-machine-support/01.1-01-SUMMARY.md`
</output>
