---
phase: 01.1-local-machine-support
plan: 03
type: execute
wave: 3
depends_on: ["01.1-02"]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/stores/machineStore.ts
  - frontend/src/components/navigation/MachineTab.tsx
  - frontend/src/components/navigation/MachineTabBar.tsx
  - frontend/src/hooks/useStatus.ts
autonomous: false

must_haves:
  truths:
    - "'This Machine' appears first in the machine tab bar on launch"
    - "'This Machine' always shows green (online) status indicator"
    - "'This Machine' cannot be deleted or edited from the UI"
    - "User can open a terminal tab on 'This Machine' with the same UX as remote machines"
    - "Local Claude Code sessions appear in the Claude overview"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "LOCAL_MACHINE_ID constant"
      contains: "LOCAL_MACHINE_ID"
    - path: "frontend/src/components/navigation/MachineTab.tsx"
      provides: "Machine tab with local machine visual distinction"
      contains: "LOCAL_MACHINE_ID"
    - path: "frontend/src/components/navigation/MachineTabBar.tsx"
      provides: "Tab bar with local machine always first"
  key_links:
    - from: "frontend/src/components/navigation/MachineTab.tsx"
      to: "frontend/src/types/index.ts"
      via: "LOCAL_MACHINE_ID import"
      pattern: "LOCAL_MACHINE_ID"
    - from: "frontend/src/hooks/useStatus.ts"
      to: "frontend/src/stores/machineStore.ts"
      via: "setMachineStatus for local machine"
      pattern: "setMachineStatus"
---

<objective>
Update the frontend to properly display and handle "This Machine" as a first-class, always-present machine entry.

Purpose: The backend (Plans 01-02) now returns "This Machine" in the machine list and supports local sessions. The frontend needs minimal changes to handle the synthetic local machine correctly: ensuring it appears first, shows as always-online, and prevents edit/delete operations. Most terminal UX works unchanged because the frontend already interacts generically with machine IDs.

Output: Updated types, machine tab bar, and status hook. Visual verification of the full local machine flow.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-local-machine-support/01.1-RESEARCH.md
@.planning/phases/01.1-local-machine-support/01.1-02-SUMMARY.md
@frontend/src/types/index.ts
@frontend/src/stores/machineStore.ts
@frontend/src/components/navigation/MachineTab.tsx
@frontend/src/components/navigation/MachineTabBar.tsx
@frontend/src/hooks/useStatus.ts
@frontend/src/components/machines/MachineItem.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend local machine support</name>
  <files>
    frontend/src/types/index.ts
    frontend/src/stores/machineStore.ts
    frontend/src/components/navigation/MachineTab.tsx
    frontend/src/components/navigation/MachineTabBar.tsx
    frontend/src/hooks/useStatus.ts
  </files>
  <action>
**`types/index.ts`**: Add the local machine constant and helper:
- Export `const LOCAL_MACHINE_ID = "local"` at the top of the file
- Add a helper: `export function isLocalMachine(machineId: string): boolean { return machineId === LOCAL_MACHINE_ID; }`
- Existing types stay unchanged -- the Machine interface already works for "This Machine" (id, name, status fields are all present in the API response)

**`stores/machineStore.ts`**: Ensure local machine stays first and can't be removed:
- Import `LOCAL_MACHINE_ID` from types
- Modify `setMachines`: ensure the machine with `id === LOCAL_MACHINE_ID` is always first in the array. Add a sort step: `machines.sort((a, b) => a.id === LOCAL_MACHINE_ID ? -1 : b.id === LOCAL_MACHINE_ID ? 1 : 0)`
- Modify `removeMachine`: skip removal if `id === LOCAL_MACHINE_ID` (guard against accidental removal)

**`components/navigation/MachineTab.tsx`**: Add visual distinction for local machine:
- Import `isLocalMachine` from types and `Monitor` (or `Laptop`) icon from `lucide-react`
- For the local machine tab: show a `Monitor` icon (16px) before the name instead of the colored status dot, since it's always online. Use a subtle color like `text-accent` for the icon.
- For remote machines: keep the existing colored status dot behavior unchanged.
- Do not show context menu / right-click options for the local machine (it can't be edited or deleted). If there's currently no context menu, this is a no-op.

**`components/navigation/MachineTabBar.tsx`**: No significant changes needed if `setMachines` sorts local machine first. The tab bar already renders `machines.map(...)`. But verify that:
- The "+" (add machine) button still works and doesn't interfere with local machine
- The Claude tab still works alongside local machine tab
- If the user has no remote machines configured, "This Machine" and "Claude" are the only tabs visible

**`hooks/useStatus.ts`**: Ensure the status WebSocket handler processes local machine status:
- Read the current file to understand how it handles `machine_status` messages
- The local machine will always report status "online" from the backend. The existing handler likely already calls `setMachineStatus(machine_id, status)` for any machine_id received. Verify this works for `machine_id="local"`.
- If the handler filters by known machine UUIDs or does UUID validation on the machine_id, relax that check to accept the string "local".
- The Claude sessions handler should already work since it processes whatever the backend sends, keyed by `machine_id`. Local Claude sessions will have `machine_id="local"`. Verify no UUID assumptions.
  </action>
  <verify>
    Read all modified files and verify:
    - `LOCAL_MACHINE_ID` is exported from types
    - machineStore sorts local machine first
    - MachineTab shows Monitor icon for local machine
    - No UUID validation blocks the "local" machine_id string
  </verify>
  <done>
    Frontend correctly displays "This Machine" as the first tab with a monitor icon, always showing as online. Remote machines appear after it with their existing status dots. The machine store prevents accidental removal of the local machine.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full local machine support: "This Machine" appears in the sidebar, terminal sessions can be opened on the local machine via PTY/tmux (or SSH in Docker mode), and local Claude Code sessions are detected.
  </what-built>
  <how-to-verify>
    1. Start the dev environment (docker compose up with dev profile, or run backend + frontend locally)
    2. Log in to Locus
    3. Verify "This Machine" appears as the FIRST tab in the machine tab bar with a monitor icon and always-online appearance
    4. Click "This Machine" tab
    5. Open a new terminal session on "This Machine" (via TmuxPicker or new session button)
    6. Verify the terminal opens and you get a working shell with full terminal UX (colors, keyboard input)
    7. Type `tmux ls` to verify the session is wrapped in tmux
    8. Verify remote machines still work (if any are configured)
    9. Check the Claude tab -- if any Claude Code sessions are running locally, they should appear
    10. Try to delete or edit "This Machine" -- it should not be possible
  </how-to-verify>
  <resume-signal>Type "approved" if the local machine flow works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- "This Machine" is always the first tab in the machine tab bar
- Terminal sessions on "This Machine" produce a working shell
- Local tmux sessions are visible and attachable
- Claude overview includes local Claude sessions
- Remote machine functionality is unaffected
- "This Machine" cannot be edited or deleted
</verification>

<success_criteria>
Phase 1.1 success criteria are met:
1. "This Machine" appears in the sidebar automatically -- no manual setup, always connected
2. User can open terminal tabs on the local machine with full terminal UX
3. User can see and attach to local tmux sessions
4. Local Claude Code sessions are detected and shown in the Claude overview
5. Local repo discovery works via filesystem scan (scan_repos endpoint handles local machine)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-local-machine-support/01.1-03-SUMMARY.md`
</output>
