---
phase: 01-infrastructure-terminal-core
plan: 06
type: execute
wave: 3
depends_on: ["01-02", "01-04"]
files_modified:
  - frontend/src/components/auth/LoginPage.tsx
  - frontend/src/components/auth/SetupWizard.tsx
  - frontend/src/components/auth/WizardStep.tsx
  - frontend/src/components/machines/MachineForm.tsx
  - frontend/src/components/machines/TmuxPicker.tsx
  - frontend/src/components/machines/RepoPicker.tsx
  - frontend/src/components/settings/SettingsPage.tsx
  - frontend/src/components/settings/MachineSettings.tsx
  - frontend/src/components/settings/CredentialSettings.tsx
  - frontend/src/components/settings/CredentialForm.tsx
  - frontend/src/components/settings/ClaudeCodeSettings.tsx
  - frontend/src/App.tsx
  - frontend/src/hooks/useApi.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - DEPL-02

must_haves:
  truths:
    - "User sees setup wizard on first visit (no user exists)"
    - "User can set a password in the setup wizard"
    - "User can add a machine with test connection in the setup wizard"
    - "User can skip optional steps in the wizard"
    - "User sees login page after setup or on return visits"
    - "User can manage machines in settings"
    - "User can manage credentials in settings with test buttons"
    - "User can configure Claude Code auth in settings"
  artifacts:
    - path: "frontend/src/components/auth/LoginPage.tsx"
      provides: "Login form"
      contains: "Log in to Locus"
    - path: "frontend/src/components/auth/SetupWizard.tsx"
      provides: "First-run setup flow"
      contains: "Set your password"
    - path: "frontend/src/components/settings/SettingsPage.tsx"
      provides: "Settings page with sections"
      contains: "Settings"
    - path: "frontend/src/components/machines/MachineForm.tsx"
      provides: "Machine add/edit form"
      contains: "host"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/auth/LoginPage.tsx"
      via: "conditional render when not authenticated"
      pattern: "LoginPage|SetupWizard"
    - from: "frontend/src/components/auth/SetupWizard.tsx"
      to: "frontend/src/api/client.ts"
      via: "POST /api/auth/setup"
      pattern: "api/auth/setup"
    - from: "frontend/src/components/settings/CredentialSettings.tsx"
      to: "frontend/src/api/client.ts"
      via: "POST /api/settings/credentials"
      pattern: "api/settings/credentials"
---

<objective>
Build the authentication UI (login page, setup wizard), machine management forms (add/edit with test connection, tmux picker, repo picker), and settings pages (machines, credentials, Claude Code config).

Purpose: Users need to be able to set up their Locus instance, log in, manage SSH machines, store encrypted credentials, and configure Claude Code auth. This is the user-facing surface for all Phase 1 auth and machine management requirements.

Output: Login page, three-step setup wizard, settings page with machine/credential/Claude sections, and supporting forms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-infrastructure-terminal-core/01-CONTEXT.md
@.planning/phases/01-infrastructure-terminal-core/01-UI-SPEC.md
@.planning/phases/01-infrastructure-terminal-core/01-02-SUMMARY.md
@.planning/phases/01-infrastructure-terminal-core/01-04-SUMMARY.md

<interfaces>
From backend API (Plan 02 + Plan 05):
- GET /api/auth/status -> { is_setup: boolean }
- POST /api/auth/setup { password } -> { access_token, token_type }
- POST /api/auth/login { password } -> { access_token, token_type }
- GET /api/machines -> MachineResponse[]
- POST /api/machines { name, host, port, username, ssh_key_path, repo_scan_paths } -> MachineResponse
- POST /api/machines/test-connection { host, port, username, ssh_key_path } -> { success, message, tmux_sessions }
- GET /api/settings/credentials -> { id, service_type, service_name }[]
- POST /api/settings/credentials { service_type, service_name, data } -> { id, service_type, service_name }
- GET /api/settings/claude-code -> { type, masked_key }

From frontend (Plan 04):
- useAuthStore: token, isSetup, isAuthenticated, setToken, clearToken, setIsSetup
- useMachineStore: machines, addMachine, setMachines
- apiFetch(path, options): Promise<Response>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login page + Setup wizard + API hook</name>
  <files>
    frontend/src/hooks/useApi.ts,
    frontend/src/components/auth/LoginPage.tsx,
    frontend/src/components/auth/SetupWizard.tsx,
    frontend/src/components/auth/WizardStep.tsx,
    frontend/src/components/machines/MachineForm.tsx,
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/stores/authStore.ts (useAuthStore interface)
    frontend/src/stores/machineStore.ts (useMachineStore interface)
    frontend/src/api/client.ts (apiFetch, getWsUrl signatures)
    frontend/src/main.css (color tokens, font tokens)
    frontend/src/App.tsx (current state)
    .planning/phases/01-infrastructure-terminal-core/01-UI-SPEC.md (copywriting contract — all copy for login, setup wizard, machine form, error messages; component inventory — LoginPage, SetupWizard, WizardStep, MachineForm states; colors, spacing, typography)
    .planning/phases/01-infrastructure-terminal-core/01-CONTEXT.md (D-09: wizard steps, D-12: machine form fields)
  </read_first>
  <action>
    **frontend/src/hooks/useApi.ts**: Simple wrapper around apiFetch for common patterns:
    ```typescript
    import { apiFetch } from "../api/client";
    import { useAuthStore } from "../stores/authStore";

    export async function apiGet<T>(path: string): Promise<T> {
      const res = await apiFetch(path);
      if (!res.ok) throw new Error(await res.text());
      return res.json();
    }

    export async function apiPost<T>(path: string, body: unknown): Promise<T> {
      const res = await apiFetch(path, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(body),
      });
      if (!res.ok) throw new Error(await res.text());
      return res.json();
    }

    export async function apiPut<T>(path: string, body: unknown): Promise<T> {
      const res = await apiFetch(path, {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(body),
      });
      if (!res.ok) throw new Error(await res.text());
      return res.json();
    }

    export async function apiDelete(path: string): Promise<void> {
      const res = await apiFetch(path, { method: "DELETE" });
      if (!res.ok) throw new Error(await res.text());
    }
    ```

    **frontend/src/components/auth/LoginPage.tsx** — per UI-SPEC:
    - Centered on page with `3xl` (64px) centering offset.
    - Background `dominant` (#0f1117).
    - "Locus" heading (18px semibold).
    - Password input field with `md` (16px) spacing.
    - Submit button: "Log in to Locus" (per UI-SPEC copy), accent background.
    - States: default, loading (button shows spinner/disabled), error ("Incorrect password. Try again." per UI-SPEC copy in red-500).
    - On submit: POST /api/auth/login with password. On success: `authStore.setToken(response.access_token)`. On 401: show error.

    **frontend/src/components/auth/WizardStep.tsx** — reusable step container:
    - Props: `heading: string`, `body: string`, `isActive: boolean`, `isCompleted: boolean`, `children: ReactNode`
    - Renders heading (18px semibold), body (14px regular muted), and children content area.
    - Visual indicators for active/completed/skipped states.

    **frontend/src/components/auth/SetupWizard.tsx** — per D-09 and UI-SPEC:
    - Three steps with progress indicator at top.
    - **Step 1 (required):** "Set your password" / "Choose a password to secure your Locus instance."
      - Password input (min 8 chars) + confirm password input.
      - "Next" button. On submit: POST /api/auth/setup with password. Store token.
    - **Step 2 (required):** "Connect your first machine" / "Add a remote machine to start working. You can add more later in Settings."
      - Renders `<MachineForm />` with onSuccess callback to advance.
      - "Skip for now" link (per UI-SPEC copy).
    - **Step 3 (optional):** "Add service credentials" / "Connect GitLab, GitHub, or other services. You can skip this and add them later."
      - Simple service type selector + credential fields.
      - "Skip for now" link.
      - "Start using Locus" button (per UI-SPEC copy) to finish wizard.
    - Background: `dominant`. Card surface: `secondary` with `border` border.
    - Spacing: `2xl` (48px) page padding, `lg` (24px) section spacing.

    **frontend/src/components/machines/MachineForm.tsx** — per D-12 and UI-SPEC:
    - Fields: Name (text), Host (text), Port (number, default 22), Username (text), SSH Key Path (text), Repo Scan Paths (comma-separated text or list input).
    - "Test connection" button: POST /api/machines/test-connection. Shows states: default, testing (spinner), success ("Connected successfully." in green), error ("Connection test failed: {error}" in red). Per UI-SPEC copywriting.
    - "Add Machine" / "Save" submit button (accent).
    - On submit: POST /api/machines with form data. Add to machineStore.
    - Form validation: name required, host required, username required, ssh_key_path required.
    - `md` (16px) spacing between fields. `touch` (36px) min height for inputs and buttons.

    **Update frontend/src/App.tsx**:
    - On mount: GET /api/auth/status. Set `authStore.isSetup`.
    - If `isSetup === null`: show loading spinner.
    - If `isSetup === false`: show `<SetupWizard />`.
    - If `isSetup === true && !isAuthenticated`: show `<LoginPage />`.
    - If `isAuthenticated`: show `<AppShell />`.
  </action>
  <verify>
    <automated>cd /home/samy/locus/frontend && for f in src/components/auth/LoginPage.tsx src/components/auth/SetupWizard.tsx src/components/auth/WizardStep.tsx src/components/machines/MachineForm.tsx src/hooks/useApi.ts; do test -f "$f" && echo "$f exists" || echo "MISSING: $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/auth/LoginPage.tsx contains `Log in to Locus` and `Incorrect password. Try again.`
    - frontend/src/components/auth/LoginPage.tsx contains `api/auth/login`
    - frontend/src/components/auth/SetupWizard.tsx contains `Set your password` and `Connect your first machine` and `Add service credentials`
    - frontend/src/components/auth/SetupWizard.tsx contains `Skip for now` and `Start using Locus`
    - frontend/src/components/auth/SetupWizard.tsx contains `api/auth/setup`
    - frontend/src/components/machines/MachineForm.tsx contains `test-connection` and `Connected successfully` and `Connection test failed`
    - frontend/src/components/machines/MachineForm.tsx contains fields for `host`, `port`, `username`, `ssh_key_path`
    - frontend/src/App.tsx contains `api/auth/status` and `SetupWizard` and `LoginPage` and `AppShell`
    - frontend/src/hooks/useApi.ts contains `apiGet`, `apiPost`, `apiPut`, `apiDelete`
  </acceptance_criteria>
  <done>Login page authenticates with JWT. Setup wizard walks through password creation, machine addition with test connection, and optional credential setup. App.tsx routes between wizard, login, and main shell based on auth state.</done>
</task>

<task type="auto">
  <name>Task 2: Settings pages (Machines, Credentials, Claude Code)</name>
  <files>
    frontend/src/components/settings/SettingsPage.tsx,
    frontend/src/components/settings/MachineSettings.tsx,
    frontend/src/components/settings/CredentialSettings.tsx,
    frontend/src/components/settings/CredentialForm.tsx,
    frontend/src/components/settings/ClaudeCodeSettings.tsx,
    frontend/src/components/machines/TmuxPicker.tsx,
    frontend/src/components/machines/RepoPicker.tsx
  </files>
  <read_first>
    frontend/src/hooks/useApi.ts (apiGet, apiPost, apiPut, apiDelete)
    frontend/src/stores/machineStore.ts (useMachineStore)
    frontend/src/components/machines/MachineForm.tsx (reuse for editing machines)
    frontend/src/main.css (color tokens)
    .planning/phases/01-infrastructure-terminal-core/01-UI-SPEC.md (component inventory for settings, copywriting for destructive actions and empty states, colors, spacing)
    .planning/phases/01-infrastructure-terminal-core/01-CONTEXT.md (D-10: extensible credential settings, D-11: Claude Code auth, D-14: tmux picker, D-04: repo picker)
  </read_first>
  <action>
    **frontend/src/components/settings/SettingsPage.tsx**:
    - Full-page layout replacing center panel content when "Settings" is selected from user menu.
    - Background: `dominant`. Sections on `secondary` surface cards with `border` border.
    - Sections: Machines, Credentials, Claude Code — each as a card with heading.
    - `lg` (24px) section padding, `xl` (32px) gap between sections.
    - Navigation via simple anchor scroll or tab-style section selector at top.

    **frontend/src/components/settings/MachineSettings.tsx**:
    - List of machines with name, host, status dot, and action buttons (Edit, Delete).
    - "Add Machine" button opens MachineForm in create mode.
    - Edit opens MachineForm in edit mode (pre-filled).
    - Delete: confirmation dialog with copy "This will close all active sessions on this machine. This cannot be undone." + machine name (per UI-SPEC destructive copy). Calls DELETE /api/machines/{id}.
    - Uses GET /api/machines to load list.

    **frontend/src/components/settings/CredentialSettings.tsx** — per D-10:
    - List of stored credentials: service_type badge, service_name, action buttons (Edit, Test, Delete).
    - "Add Credential" button opens CredentialForm.
    - Delete: confirmation "This credential will be permanently removed." (per UI-SPEC copy).
    - Test button: POST /api/settings/credentials/{id}/test. Shows success/error inline.
    - Design must be extensible — service_type is a string, not a fixed enum. New types from Phase 4 Integrator skill should work without UI changes.

    **frontend/src/components/settings/CredentialForm.tsx**:
    - Service type selector: dropdown with options "gitlab", "github", "jira", "google", "other".
    - Service name: text input.
    - Credential fields: dynamic based on service_type.
      - gitlab/github: `token` (password input)
      - jira: `email` (text) + `api_token` (password)
      - google: `credentials_json` (textarea)
      - other: `key` (text) + `value` (password)
    - "Test connection" button if available.
    - Save button: POST /api/settings/credentials with `{service_type, service_name, data: {fields...}}`.

    **frontend/src/components/settings/ClaudeCodeSettings.tsx** — per D-11:
    - Shows current config status: "configured" or "unconfigured".
    - Auth type selector: "API Key" or "OAuth".
    - For API Key: single password input.
    - Save button: PUT /api/settings/claude-code.
    - "Push to machine" button per connected machine (POST /api/settings/claude-code/push/{machine_id}). This implements AUTH-04 from the UI side.

    **frontend/src/components/machines/TmuxPicker.tsx** — per D-14:
    - Modal/dialog showing list of tmux sessions on the machine.
    - Each row: session name, attached status, last activity timestamp.
    - "Attach" button per session.
    - "Create new session" button at bottom.
    - Loading state while fetching.
    - Empty state: auto-creates new session (per D-14 — "If none found, create new tmux session automatically").

    **frontend/src/components/machines/RepoPicker.tsx** — per D-04:
    - Quick picker modal/dropdown listing repos known to the machine.
    - Each row: repo name (basename of path), full path.
    - "Open plain shell" option at bottom.
    - Loading state while fetching from GET /api/machines/{id}/repos.
  </action>
  <verify>
    <automated>cd /home/samy/locus/frontend && for f in src/components/settings/SettingsPage.tsx src/components/settings/MachineSettings.tsx src/components/settings/CredentialSettings.tsx src/components/settings/CredentialForm.tsx src/components/settings/ClaudeCodeSettings.tsx src/components/machines/TmuxPicker.tsx src/components/machines/RepoPicker.tsx; do test -f "$f" && echo "$f exists" || echo "MISSING: $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/settings/SettingsPage.tsx contains sections for Machines, Credentials, Claude Code
    - frontend/src/components/settings/MachineSettings.tsx contains `api/machines` and delete confirmation copy "This will close all active sessions"
    - frontend/src/components/settings/CredentialSettings.tsx contains `api/settings/credentials` and "This credential will be permanently removed"
    - frontend/src/components/settings/CredentialForm.tsx contains service_type options including `gitlab`, `github`, `jira`
    - frontend/src/components/settings/ClaudeCodeSettings.tsx contains `api/settings/claude-code` and `push` endpoint reference
    - frontend/src/components/machines/TmuxPicker.tsx contains `tmux` session list rendering
    - frontend/src/components/machines/RepoPicker.tsx contains `repos` API call and "Open plain shell" option
  </acceptance_criteria>
  <done>Settings page provides machine list with add/edit/delete, credential management with per-type forms and test buttons, Claude Code auth config with push-to-machine. TmuxPicker shows existing sessions. RepoPicker lists repos with plain shell fallback.</done>
</task>

</tasks>

<verification>
- All auth and settings components render with correct copy from UI-SPEC
- App.tsx properly routes between setup wizard, login, and main shell
- Settings page accessible from user menu
- All API endpoints called correctly from frontend
</verification>

<success_criteria>
- Setup wizard completes: password -> machine -> credentials (optional) -> main app
- Login page authenticates and stores JWT
- Settings page allows CRUD for machines and credentials
- Claude Code settings show config status and push-to-machine
- All UI copy matches UI-SPEC copywriting contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-terminal-core/01-06-SUMMARY.md`
</output>
