---
phase: 02-repository-management-work-feed
plan: 07
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - frontend/src/stores/feedStore.ts
  - frontend/src/hooks/useFeedWebSocket.ts
  - frontend/src/components/feed/FeedPanel.tsx
  - frontend/src/components/feed/FeedTierSection.tsx
  - frontend/src/components/feed/FeedCard.tsx
  - frontend/src/components/feed/SnoozeMenu.tsx
  - frontend/src/components/layout/RightPanel.tsx
autonomous: true

must_haves:
  truths:
    - "User sees feed items grouped into 5 urgency tiers with collapsible sections and sticky headers"
    - "Now and Respond tiers are expanded by default; Prep and Follow up are collapsed"
    - "Hover on a card reveals Quick Promote, Deep Promote, Snooze, Dismiss actions"
    - "Snooze offers fixed presets: 1h, 4h, Tomorrow 9am, Next Monday 9am"
    - "Feed updates in real-time via WebSocket without page refresh"
  artifacts:
    - path: "frontend/src/components/feed/FeedPanel.tsx"
      provides: "Feed tab content with tier sections"
      contains: "FeedPanel"
    - path: "frontend/src/components/feed/FeedTierSection.tsx"
      provides: "Collapsible tier with sticky header and tier color"
      contains: "FeedTierSection"
    - path: "frontend/src/components/feed/FeedCard.tsx"
      provides: "Feed item card with hover triage actions"
      contains: "FeedCard"
    - path: "frontend/src/stores/feedStore.ts"
      provides: "Zustand store for feed state"
      contains: "useFeedStore"
    - path: "frontend/src/hooks/useFeedWebSocket.ts"
      provides: "WebSocket connection triggering TanStack Query invalidation"
      contains: "useFeedWebSocket"
  key_links:
    - from: "frontend/src/hooks/useFeedWebSocket.ts"
      to: "/ws/feed"
      via: "WebSocket connection"
      pattern: "ws.*feed"
    - from: "frontend/src/hooks/useFeedWebSocket.ts"
      to: "queryClient.invalidateQueries"
      via: "cache invalidation on WS message"
      pattern: "invalidateQueries.*feed"
    - from: "frontend/src/components/layout/RightPanel.tsx"
      to: "FeedPanel"
      via: "Tab rendering"
      pattern: "FeedPanel"
---

<objective>
Build the feed panel with tiered sections, feed cards, triage actions, snooze, and real-time WebSocket updates.

Purpose: The feed is the right-panel inbox where work signals arrive from all sources. Users triage items by promoting to tasks, snoozing, or dismissing. Real-time updates ensure nothing is missed.
Output: Feed panel with 5 tier sections, card with hover actions, snooze presets, WebSocket integration, RightPanel with Feed/Board tab toggle.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-management-work-feed/02-CONTEXT.md
@.planning/phases/02-repository-management-work-feed/02-RESEARCH.md
@frontend/src/components/layout/RightPanel.tsx
@frontend/src/stores/panelStore.ts
@frontend/src/hooks/useApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feed store, WebSocket hook, and RightPanel tab structure</name>
  <files>
    frontend/src/stores/feedStore.ts
    frontend/src/hooks/useFeedWebSocket.ts
    frontend/src/components/layout/RightPanel.tsx
  </files>
  <action>
**feedStore.ts** -- Zustand store:
- State: activeTab ("feed" | "board"), tierCollapsed (Record<string, boolean> with defaults: now=false, respond=false, review=true initially but show all, prep=true, follow_up=true -- per LOCKED: Now+Respond expanded, Prep+Follow up collapsed; Review starts expanded)
- Actions: setActiveTab, toggleTierCollapsed(tier)

**useFeedWebSocket.ts** -- Follow RESEARCH.md Pattern 3:
- Connect to /ws/feed with auth token (same pattern as existing useWebSocket hooks)
- On message: parse JSON, if type is "new_item" or "item_updated" or "item_dismissed", call `queryClient.invalidateQueries({ queryKey: ['feed'] })`
- Handle reconnection (follow existing WebSocket patterns in the codebase)
- Return connection status for optional display

Also create TanStack Query hooks in same file or a separate useFeedQueries.ts:
- `useFeedItems(tier?: string)`: GET /api/feed/ with optional tier filter. Returns FeedItem[].
- `useDismissItem()`: useMutation PATCH /api/feed/{id} with {is_dismissed: true}. Invalidate feed queries on success.
- `useSnoozeItem()`: useMutation POST /api/feed/{id}/snooze. Invalidate feed queries.
- `useMarkRead()`: useMutation PATCH /api/feed/{id} with {is_read: true}.
- `usePromoteItem()`: useMutation POST /api/tasks/promote. Invalidate both feed and tasks queries.

**RightPanel.tsx** -- Replace placeholder with tab structure:
- Header with two tab buttons: "Feed" and "Board" (per LOCKED: right panel has Feed and Board tabs)
- Active tab highlighted with accent color
- Render FeedPanel when activeTab="feed", BoardPanel when activeTab="board" (BoardPanel placeholder for now)
- Use feedStore.activeTab for state
- Style: bg-secondary, border-l border-border, 340px equivalent width (controlled by parent Panel in AppShell)
  </action>
  <verify>
TypeScript compilation: `cd /c/Users/USER/locus/frontend && npx tsc --noEmit 2>&1 | head -20`
  </verify>
  <done>Feed store manages tab state and tier collapse. WebSocket hook connects to /ws/feed and invalidates TanStack Query cache on updates. RightPanel shows Feed/Board tab toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Build feed panel with tier sections, cards, and triage actions</name>
  <files>
    frontend/src/components/feed/FeedPanel.tsx
    frontend/src/components/feed/FeedTierSection.tsx
    frontend/src/components/feed/FeedCard.tsx
    frontend/src/components/feed/SnoozeMenu.tsx
  </files>
  <action>
**FeedPanel.tsx**:
- Uses useFeedItems() to get all feed items
- Groups items by tier (now, respond, review, prep, follow_up)
- Renders 5 FeedTierSection components in order
- Activates useFeedWebSocket() for real-time updates
- Single vertical scroll axis (per LOCKED: no horizontal scrolling)
- Empty state: "All clear -- nothing needs attention" message (zen message per LOCKED decisions, since sources may not be configured yet show setup prompt if no integration sources exist)

**FeedTierSection.tsx**:
- Props: tier name, items[], isCollapsed, onToggle
- Sticky header with tier name and count badge (per LOCKED: sticky tier headers)
- Tier color accent on header: Now=red, Respond=orange, Review=blue, Prep=yellow, Follow up=gray
- Collapse/expand chevron
- Renders FeedCard for each item when expanded
- Smooth collapse animation (max-height transition)

**FeedCard.tsx**:
- Props: FeedItem
- Default view: source icon (lucide icon based on source_type: github=Github, gitlab=GitlabIcon or generic, jira=Ticket, calendar=Calendar), title (single line, truncate), relative timestamp, unread dot (blue dot if !is_read), 1-line snippet
- Tier accent: colored left border (4px) matching tier color
- Hover state: snippet row transforms to show 4 action buttons: Quick Promote (arrow-up icon), Deep Promote (sparkles icon), Snooze (clock icon), Dismiss (x icon)
- Quick Promote: calls usePromoteItem mutation with auto-generated title/context from feed item
- Deep Promote: opens modal (implement basic modal -- LLM integration is Claude's discretion, for now show editable title + context textarea + confirm)
- Snooze: opens SnoozeMenu
- Dismiss: calls useDismissItem mutation, card fades out
- Click (not on actions): marks as read, could expand to show full snippet
- Compact layout: text-xs for metadata, text-sm for title, px-2 py-1.5 padding per visual design decisions

**SnoozeMenu.tsx**:
- Small popover/dropdown with 4 preset options (per LOCKED):
  - "1 hour" -> now + 1h
  - "4 hours" -> now + 4h
  - "Tomorrow 9am" -> next day 9:00 AM local time
  - "Next Monday 9am" -> next Monday 9:00 AM local time
- On select: calls useSnoozeItem mutation with calculated datetime
- Style: bg-dominant, border, shadow, text-xs

All components: dark theme Tailwind classes, compact design, 10px metadata font size where specified.
  </action>
  <verify>
Run: `cd /c/Users/USER/locus/frontend && npm run build 2>&1 | tail -5` to verify build succeeds.
  </verify>
  <done>Feed panel shows items in 5 collapsible tier sections with sticky headers. Cards show source icon, title, timestamp, unread dot, snippet. Hover reveals Quick Promote, Deep Promote, Snooze, Dismiss. Snooze offers 4 fixed presets. Real-time updates via WebSocket.</done>
</task>

</tasks>

<verification>
- 5 tier sections render in correct order (Now -> Follow up)
- Now+Respond expanded, Prep+Follow up collapsed by default
- Cards show colored left border matching tier
- Hover replaces snippet with action buttons
- Snooze menu shows 4 presets with correct time calculations
- WebSocket invalidates queries on new/updated items
- Feed/Board tab toggle works in RightPanel
</verification>

<success_criteria>
Complete feed panel matching all locked visual and interaction decisions. Users can view, triage, and promote feed items. Real-time updates keep the feed current.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-management-work-feed/02-07-SUMMARY.md`
</output>
