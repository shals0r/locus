---
phase: 02-repository-management-work-feed
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - backend/app/api/git.py
  - backend/app/api/feed.py
  - backend/app/api/gsd_events.py
  - backend/app/api/__init__.py
  - backend/app/ws/feed.py
  - backend/app/ws/__init__.py
  - backend/app/main.py
autonomous: true

must_haves:
  truths:
    - "GET /api/git/status returns repo status for a given machine+repo"
    - "GET /api/git/gsd-state returns GSD phase/progress data for a repo"
    - "POST /api/git/fetch|pull|push performs git operations and returns result"
    - "POST /api/feed/ingest accepts webhook payloads with HMAC verification"
    - "POST /api/feed/transcripts accepts transcript text and extracts action items"
    - "GET /api/feed returns feed items filtered by tier and sorted by urgency"
    - "WebSocket /ws/feed pushes real-time feed updates to connected clients"
    - "POST /api/gsd-events accepts GSD event payloads and emits them as feed items"
  artifacts:
    - path: "backend/app/api/git.py"
      provides: "Git REST endpoints including GSD state"
      exports: ["router"]
    - path: "backend/app/api/feed.py"
      provides: "Feed CRUD + ingest webhook + transcript submission endpoint"
      exports: ["router"]
    - path: "backend/app/api/gsd_events.py"
      provides: "GSD event submission endpoint (FEED-06)"
      exports: ["router"]
    - path: "backend/app/ws/feed.py"
      provides: "Feed WebSocket with broadcast"
      contains: "broadcast_feed_update"
  key_links:
    - from: "backend/app/api/git.py"
      to: "backend/app/services/git_service.py"
      via: "service function calls"
      pattern: "from app\\.services\\.git_service import"
    - from: "backend/app/api/feed.py"
      to: "backend/app/services/feed_service.py"
      via: "service function calls"
      pattern: "from app\\.services\\.feed_service import"
    - from: "backend/app/api/gsd_events.py"
      to: "backend/app/services/gsd_event_service.py"
      via: "emit_gsd_event call"
      pattern: "from app\\.services\\.gsd_event_service import"
    - from: "backend/app/ws/feed.py"
      to: "broadcast_feed_update"
      via: "asyncio.Queue broadcast list"
      pattern: "feed_clients"
---

<objective>
Create git and feed REST API routes, GSD event submission endpoint, plus the feed WebSocket endpoint. Includes GSD state endpoint and transcript submission.

Purpose: Backend API surface for git operations (including GSD state), feed management, transcript processing, and GSD event ingestion. The git API serves the sidebar UI; the feed API + WebSocket serve the right panel feed; the GSD events endpoint lets the frontend report GSD command completions as feed items.
Output: 3 API routers (git, feed, gsd_events), 1 WebSocket handler (feed), all registered in main.py.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-management-work-feed/02-RESEARCH.md
@backend/app/api/machines.py
@backend/app/ws/status.py
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create git API and feed API routes (including GSD state and transcript endpoints)</name>
  <files>
    backend/app/api/git.py
    backend/app/api/feed.py
    backend/app/api/__init__.py
  </files>
  <action>
**git.py** (APIRouter prefix="/api/git"):
- `GET /status?machine_id=&repo_path=` -> RepoStatus. Calls git_service.get_repo_status.
- `GET /status/all?machine_id=` -> list[RepoDetail]. Gets status for all repos on a machine (reads repos from machine's repo_scan_paths, scans them, returns status for each). Uses existing repo scanning pattern from api/machines.py.
- `GET /commits?machine_id=&repo_path=&limit=30` -> list[CommitEntry]. Calls git_service.get_commit_log.
- `GET /changed-files?machine_id=&repo_path=` -> list[ChangedFile]. Calls git_service.get_changed_files.
- `GET /branches?machine_id=&repo_path=` -> list[BranchInfo]. Calls git_service.list_branches.
- `POST /fetch` (body: {machine_id, repo_path}) -> GitOpResult. Calls git_service.git_fetch. Wrap in try/except, return success/error.
- `POST /pull` (body: same) -> GitOpResult. Same pattern.
- `POST /push` (body: same) -> GitOpResult. Same pattern.
- `POST /checkout` (body: {machine_id, repo_path, branch}) -> GitOpResult. Calls checkout_branch.
- `POST /create-branch` (body: {machine_id, repo_path, branch}) -> GitOpResult. Calls create_branch.
- `GET /diff?machine_id=&repo_path=&file_path=` -> {diff: str}. Calls get_diff_for_file.
- `GET /commit-diff?machine_id=&repo_path=&sha=` -> {diff: str}. Calls get_commit_diff.
- **`GET /gsd-state?machine_id=&repo_path=` -> GsdState (GIT-05)**. Calls git_service.get_gsd_state(). Returns {has_gsd, current_phase, phase_status, pending_todos, blockers, total_phases, completed_phases}. This powers the GSD state display in the sidebar and the GSD action buttons.

All endpoints require auth via `Depends(get_current_user)` matching existing api routes.

**feed.py** (APIRouter prefix="/api/feed"):
- `POST /ingest` -> {status, item_id}. Webhook ingest with HMAC verification (see RESEARCH.md webhook example). Check X-Locus-Signature header; if present, verify HMAC-SHA256 against source's webhook_secret. If no signature, require JWT auth. After ingest, call broadcast_feed_update.
- `GET /` -> FeedListResponse. Query params: tier (optional), include_dismissed (bool, default false). Returns items sorted by tier priority then created_at desc.
- `GET /{item_id}` -> FeedItemResponse.
- `PATCH /{item_id}` -> FeedItemResponse. Body: FeedItemUpdate. For read/dismiss/snooze/tier changes.
- `POST /{item_id}/snooze` -> FeedItemResponse. Body: {until: datetime}. Preset calculation happens on frontend.
- `POST /{item_id}/dismiss` -> FeedItemResponse.
- `DELETE /{item_id}` -> {ok: true}. Hard delete (admin cleanup).
- **`POST /transcripts` -> {status, items: list[FeedItemResponse]} (FEED-07)**. Body: {transcript_text: str, meeting_title: str | None, meeting_date: str | None}. Accepts raw meeting transcript text. Uses LLM (same httpx pattern as classify_tier) to extract action items from the transcript. Prompt: "Extract action items from this meeting transcript. For each, provide: title (brief), context (1-2 sentences), and urgency (now/respond/review/prep/follow_up). Return JSON array of {title, context, urgency}. Transcript: {transcript_text}". Parse the JSON response. For each extracted action item, call feed_service.ingest_item() with source_type="transcript", external_id="transcript:{hash(title+meeting_date)}", tier_hint=urgency from LLM. Return list of created feed items. If LLM fails, create a single feed item with the full transcript as snippet and title=meeting_title or "Meeting transcript", tier="review". Requires JWT auth.

Register both routers in api/__init__.py and include them in main.py app.
  </action>
  <verify>
Run the app and test: `curl -s http://localhost:8000/api/feed/ -H "Authorization: Bearer <token>" | head -c 200` should return JSON (empty list initially).
Verify routes registered: `curl -s http://localhost:8000/openapi.json | python -c "import sys,json; paths=json.load(sys.stdin)['paths']; [print(p) for p in paths if '/git/' in p or '/feed/' in p]"` -- should include /api/git/gsd-state and /api/feed/transcripts.
  </verify>
  <done>All git and feed API endpoints registered and responding. Git endpoints include GSD state endpoint. Feed ingest supports HMAC verification. Transcript endpoint extracts action items via LLM. Feed CRUD supports filtering and triage actions.</done>
</task>

<task type="auto">
  <name>Task 2: Create GSD events endpoint, feed WebSocket, and register all in main.py</name>
  <files>
    backend/app/api/gsd_events.py
    backend/app/ws/feed.py
    backend/app/ws/__init__.py
    backend/app/main.py
  </files>
  <action>
**api/gsd_events.py** (APIRouter prefix="/api/gsd-events") -- **FEED-06 wiring**:
- `POST /` -> {status, item_id}. Body: {event_type: str, repo_path: str, machine_id: str, phase: str | None, message: str | None}. Requires JWT auth via `Depends(get_current_user)`. Calls `gsd_event_service.emit_gsd_event(db, event_type, repo_path, machine_id, details={"phase_name": phase, "message": message})`. Returns the created feed item ID. This endpoint is the bridge for FEED-06: the frontend's GsdActions.tsx (Plan 06) calls this endpoint fire-and-forget after dispatching a GSD command in the terminal, so GSD workflow events appear in the feed.
- Accepted event_types: "phase_planned", "phase_executed", "phase_verified", "gap_found", "phase_complete"
- Reject unknown event_types with 400.

**ws/feed.py** -- Follow the EXACT pattern from ws/status.py:
1. Module-level `feed_clients: list[asyncio.Queue] = []`
2. `broadcast_feed_update(update: dict)` function that pushes to all client queues (with try/except QueueFull)
3. WebSocket endpoint handler:
   - Auth via `?token=` query param (same as /ws/terminal and /ws/status)
   - On connect: send initial snapshot of recent unread feed items (last 50, not dismissed, not snoozed)
   - Create bounded asyncio.Queue(maxsize=100) for this client
   - Add to feed_clients list
   - Loop: await queue.get(), send JSON to client
   - Finally block: remove queue from feed_clients

Message format: `{"type": "new_item" | "item_updated" | "item_dismissed", "item_id": "uuid-str"}`

**main.py updates:**
- Import and include git router, feed router, and gsd_events router
- Import feed WebSocket and add route
- The routers should follow the same pattern as existing machines and sessions routers

**ws/__init__.py:** Export feed websocket handler.

IMPORTANT: The broadcast_feed_update function must be importable by feed API routes (api/feed.py) so it can broadcast after ingest. Import path: `from app.ws.feed import broadcast_feed_update`.
  </action>
  <verify>
Verify WebSocket endpoint exists: check that `/ws/feed` route is registered in main.py.
Verify GSD events endpoint: `curl -s http://localhost:8000/openapi.json | python -c "import sys,json; paths=json.load(sys.stdin)['paths']; [print(p) for p in paths if 'gsd' in p]"` -- should show /api/gsd-events/.
Run: `cd /c/Users/USER/locus && docker compose exec app python -c "from app.ws.feed import broadcast_feed_update; print('Feed WS OK')"`
  </verify>
  <done>GSD events endpoint accepts event payloads and creates feed items via gsd_event_service (FEED-06 wired). Feed WebSocket endpoint handles client connections with auth, sends initial snapshot, receives broadcast updates from feed service. Broadcast function importable by API routes.</done>
</task>

</tasks>

<verification>
- Git API routes all registered at /api/git/*
- GSD state endpoint at /api/git/gsd-state returns GSD phase/progress data
- Feed API routes all registered at /api/feed/*
- Transcript endpoint at /api/feed/transcripts accepts text and extracts actions
- GSD events endpoint at /api/gsd-events/ accepts event payloads (FEED-06)
- Feed WebSocket at /ws/feed
- HMAC verification on ingest endpoint
- broadcast_feed_update importable from ws.feed
- All routes require auth (except ingest with valid HMAC)
</verification>

<success_criteria>
Complete backend API surface for git operations (including GSD state), feed management, transcript processing, and GSD event ingestion. Frontend can call these endpoints for all git sidebar and feed panel interactions. GSD event service is now reachable via REST API for frontend fire-and-forget calls.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-management-work-feed/02-03-SUMMARY.md`
</output>
