---
phase: 02-repository-management-work-feed
plan: 08
type: execute
wave: 4
depends_on: ["02-04", "02-06", "02-07"]
files_modified:
  - frontend/src/stores/taskStore.ts
  - frontend/src/components/board/BoardPanel.tsx
  - frontend/src/components/board/TaskColumn.tsx
  - frontend/src/components/board/TaskCard.tsx
  - frontend/src/components/board/StartFlowPicker.tsx
  - frontend/src/components/session/ContextStrip.tsx
  - frontend/src/components/layout/CenterPanel.tsx
autonomous: true

must_haves:
  truths:
    - "User sees Queue/Active/Done as vertical stacked sections on the Board tab"
    - "Queue tasks show tier color accent and float red/now items to top"
    - "User can start a task by picking machine -> repo -> optional new branch"
    - "Starting a task moves it to Active and shows a pinned context strip above center panel"
    - "Context strip shows tier dot, title, Active badge, collapsible body with source links and Copy context button"
    - "User can complete tasks via context strip or board"
  artifacts:
    - path: "frontend/src/components/board/BoardPanel.tsx"
      provides: "Board tab with 3 vertical stacked task columns"
      contains: "BoardPanel"
    - path: "frontend/src/components/board/StartFlowPicker.tsx"
      provides: "Machine -> repo -> branch inline picker"
      contains: "StartFlowPicker"
    - path: "frontend/src/components/session/ContextStrip.tsx"
      provides: "Pinned context strip above center panel"
      contains: "ContextStrip"
    - path: "frontend/src/stores/taskStore.ts"
      provides: "Zustand store for task board state"
      contains: "useTaskStore"
  key_links:
    - from: "frontend/src/components/board/StartFlowPicker.tsx"
      to: "/api/tasks/{id}/transition"
      via: "PATCH with status=active + machine_id/repo_path/branch"
      pattern: "transition"
    - from: "frontend/src/components/session/ContextStrip.tsx"
      to: "frontend/src/stores/taskStore.ts"
      via: "activeTask state"
      pattern: "useTaskStore.*activeTask"
    - from: "frontend/src/components/layout/CenterPanel.tsx"
      to: "frontend/src/components/session/ContextStrip.tsx"
      via: "rendered above tab bar when task is active"
      pattern: "ContextStrip"
---

<objective>
Build the task board (Queue/Active/Done), the Start flow (machine/repo/branch picker), the working session context strip, and integrate with center panel.

Purpose: The board is where feed items become actionable work. The Start flow creates a working context (machine + repo + branch). The context strip keeps task context visible throughout the session.
Output: Board panel with 3 columns, task cards, start flow picker, context strip, center panel integration.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-management-work-feed/02-CONTEXT.md
@.planning/phases/02-repository-management-work-feed/02-RESEARCH.md
@frontend/src/components/layout/CenterPanel.tsx
@frontend/src/stores/machineStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build task store, board panel, task cards</name>
  <files>
    frontend/src/stores/taskStore.ts
    frontend/src/components/board/BoardPanel.tsx
    frontend/src/components/board/TaskColumn.tsx
    frontend/src/components/board/TaskCard.tsx
  </files>
  <action>
**taskStore.ts** -- Zustand store:
- State: activeTask (Task | null -- the currently started task), startFlowTaskId (string | null -- which task is showing the start picker)
- Actions: setActiveTask(task | null), setStartFlowTaskId(id | null), clearActiveTask()

Also create TanStack Query hooks (in this file or hooks/useTaskQueries.ts):
- `useTasks(status?: string)`: GET /api/tasks/ with optional status filter. Returns Task[].
- `useTransitionTask()`: useMutation PATCH /api/tasks/{id}/transition. On success, invalidate tasks + update activeTask if transitioning to "active".
- `useCreateTask()`: useMutation POST /api/tasks/. Invalidate tasks.

**BoardPanel.tsx**:
- 3 vertical stacked sections (per LOCKED: NOT horizontal columns -- stacked vertically because right panel is only 340px wide)
- Sections: Queue (header + cards), Active (header + cards), Done (header + cards)
- Each section collapsible with count badge
- Queue and Active expanded by default, Done collapsed by default
- Single vertical scroll
- Render TaskColumn for each status

**TaskColumn.tsx**:
- Props: status, tasks[], isCollapsed, onToggle
- Section header with status name (Queue/Active/Done) + count
- Renders TaskCard for each task
- Queue: sorted by tier priority (now items at top per LOCKED decision)
- Active: sorted by started_at
- Done: sorted by completed_at desc

**TaskCard.tsx**:
- Tier accent: colored left border (same tier colors as feed)
- Source icon (if linked to feed item), title, brief context snippet (1 line)
- Hover actions vary by status:
  - Queue: "Start" button (opens StartFlowPicker), Edit, Drop (transitions to done)
  - Active: "Complete" button (transitions to done), Edit
  - Done: strikethrough title, completion timestamp, fade after 24h (per Claude's discretion: use opacity-40 when completed_at > 24h ago)
- Compact design matching feed cards (text-xs metadata, text-sm title, px-2 py-1.5)
  </action>
  <verify>
TypeScript compilation: `cd /c/Users/USER/locus/frontend && npx tsc --noEmit 2>&1 | head -20`
  </verify>
  <done>Board shows Queue/Active/Done as vertical stacked sections. Task cards show tier accent, title, context. Hover reveals Start/Complete/Edit/Drop actions. Done cards fade after 24h.</done>
</task>

<task type="auto">
  <name>Task 2: Build Start flow picker, context strip, and CenterPanel integration</name>
  <files>
    frontend/src/components/board/StartFlowPicker.tsx
    frontend/src/components/session/ContextStrip.tsx
    frontend/src/components/layout/CenterPanel.tsx
  </files>
  <action>
**StartFlowPicker.tsx** (per LOCKED: inline picker within the board):
- Shows inline below the task card when startFlowTaskId matches
- Step 1: Select machine -- list available machines from machineStore with connection status indicators. Only online machines selectable.
- Step 2: Select repo -- list repos on selected machine (from repoStore or fetch from API). Shows current branch for each.
- Step 3 (optional): Create new branch from task title (sanitize: lowercase, replace spaces with hyphens, strip special chars). Text input pre-filled with suggested branch name. Can skip to use current branch.
- "Start session" button: calls useTransitionTask with status="active", machine_id, repo_path, branch. Sets activeTask in taskStore. Closes the picker. Opens a terminal tab cd'd to the repo (dispatch to session store).
- "Cancel" link to close without starting.
- Compact, fits within 340px panel width.

**ContextStrip.tsx** (per LOCKED: pinned context strip above center panel):
- Visible only when taskStore.activeTask is set
- Default (collapsed) view: tier color dot, task title, "Active" badge (green), collapse/expand chevron
- Expanded view adds: context brief (from task.context), source tags (feed item source_type + URL as clickable links), machine/repo/branch info, "Copy context" button
- "Copy context" button: copies formatted text to clipboard -- "Task: {title}\nContext: {context}\nBranch: {branch}\nRepo: {repo_path}\nSource: {url}" -- designed for pasting into Claude Code or ChatGPT (per LOCKED decision)
- "Complete" button in the strip (per LOCKED: mark done via context strip). Calls transition to "done", clears activeTask.
- Collapsible so it doesn't eat screen space (per LOCKED decision)
- Height: ~36px collapsed, ~120px expanded
- Style: bg-secondary, border-b border-border, px-3 py-2

**CenterPanel.tsx** -- Modify existing to include context strip:
- Render ContextStrip above existing SessionTabBar when activeTask exists
- Rest of center panel unchanged (terminal tabs, etc.)
- Context strip sits between TopBar and tab bar visually
  </action>
  <verify>
Run: `cd /c/Users/USER/locus/frontend && npm run build 2>&1 | tail -5`
  </verify>
  <done>Start flow lets user pick machine -> repo -> branch. Starting task shows pinned context strip above center panel with task info, source links, and Copy context button. Complete button on strip transitions task to done.</done>
</task>

</tasks>

<verification>
- Board tab shows Queue/Active/Done sections
- Queue sorted by tier priority (red/now at top)
- Start flow: machine -> repo -> branch picker inline on board
- Starting task: moves to Active, shows context strip in center panel
- Context strip: collapsible, shows task brief + source links + Copy context
- Complete: via context strip button or board hover action
- Done cards fade after 24h
</verification>

<success_criteria>
Complete task board with start flow and working session. User can promote feed items to tasks, start them on a specific machine/repo/branch, see context while working, and mark them complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-management-work-feed/02-08-SUMMARY.md`
</output>
