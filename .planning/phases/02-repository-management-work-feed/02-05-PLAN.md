---
phase: 02-repository-management-work-feed
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/app/integrations/__init__.py
  - backend/app/integrations/scheduler.py
  - backend/app/integrations/base_adapter.py
  - backend/app/integrations/github_adapter.py
  - backend/app/integrations/gitlab_adapter.py
  - backend/app/integrations/jira_adapter.py
  - backend/app/integrations/calendar_adapter.py
  - backend/app/main.py
autonomous: true

must_haves:
  truths:
    - "APScheduler starts in FastAPI lifespan and polls enabled integration sources on their configured intervals"
    - "Each adapter fetches items from its external API and ingests them into the feed via feed_service"
    - "Adapters can be enabled/disabled and configured per IntegrationSource record"
  artifacts:
    - path: "backend/app/integrations/scheduler.py"
      provides: "APScheduler AsyncIOScheduler lifecycle management"
      contains: "AsyncIOScheduler"
    - path: "backend/app/integrations/base_adapter.py"
      provides: "Abstract base class for polling adapters"
      contains: "class BasePollingAdapter"
    - path: "backend/app/integrations/github_adapter.py"
      provides: "GitHub PR/review/CI polling"
      contains: "class GitHubAdapter"
    - path: "backend/app/integrations/gitlab_adapter.py"
      provides: "GitLab MR/review/CI polling"
      contains: "class GitLabAdapter"
  key_links:
    - from: "backend/app/integrations/scheduler.py"
      to: "backend/app/main.py"
      via: "lifespan start_polling/stop_polling"
      pattern: "start_polling|stop_polling"
    - from: "backend/app/integrations/base_adapter.py"
      to: "backend/app/services/feed_service.py"
      via: "ingest_item call in execute()"
      pattern: "feed_service\\.ingest_item"
---

<objective>
Build the integrations polling system with APScheduler and all four Phase 2 adapters.

Purpose: Adapters are the primary feed source -- they poll GitHub, GitLab, Jira, and Google Calendar at configurable intervals and ingest items into the feed. Without these, the feed is empty unless someone sends webhooks manually.
Output: Scheduler lifecycle, base adapter class, 4 concrete adapters (GitHub, GitLab, Jira, Calendar), integrated into FastAPI lifespan.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-management-work-feed/02-RESEARCH.md
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scheduler and base adapter</name>
  <files>
    backend/app/integrations/__init__.py
    backend/app/integrations/scheduler.py
    backend/app/integrations/base_adapter.py
  </files>
  <action>
Create the `backend/app/integrations/` package.

**scheduler.py** (follow RESEARCH.md Pattern 5):
- Module-level `scheduler = AsyncIOScheduler()`
- `async def start_polling()`: Query enabled IntegrationSources from DB. For each, look up adapter via `get_adapter(source_type)`. Add interval job with `source.poll_interval_seconds`. Use `replace_existing=True`. Start scheduler. Log which adapters started.
- `async def stop_polling()`: `scheduler.shutdown(wait=False)`.
- `def get_adapter(source_type: str) -> BasePollingAdapter | None`: Registry dict mapping source_type to adapter instance. Return None for unknown types.

Default poll intervals (per Claude's Discretion):
- GitHub/GitLab: 300s (5 min)
- Jira: 300s (5 min)
- Google Calendar: 600s (10 min)

**base_adapter.py** (follow RESEARCH.md Polling Adapter Base Class):
- Abstract class `BasePollingAdapter` with:
  - `async def poll(self, source: IntegrationSource) -> list[dict]`: Abstract. Returns dicts matching IngestPayload schema.
  - `async def execute(self, source: IntegrationSource)`: Concrete. Calls self.poll(), then for each item calls feed_service.ingest_item via a fresh async session. Updates source.last_polled_at. Broadcasts feed updates for new items. Wraps everything in try/except to prevent scheduler job from dying on errors -- log errors but don't raise.

**main.py updates**: Add `start_polling()` and `stop_polling()` to the existing lifespan context manager.

**__init__.py**: Export scheduler functions.
  </action>
  <verify>
Run: `cd /c/Users/USER/locus && docker compose exec app python -c "from app.integrations.scheduler import start_polling, stop_polling, scheduler; print('Scheduler OK')"`
  </verify>
  <done>APScheduler integrated into FastAPI lifespan. Base adapter handles poll->ingest->broadcast flow with error isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Create four polling adapters (GitHub, GitLab, Jira, Calendar)</name>
  <files>
    backend/app/integrations/github_adapter.py
    backend/app/integrations/gitlab_adapter.py
    backend/app/integrations/jira_adapter.py
    backend/app/integrations/calendar_adapter.py
  </files>
  <action>
Implement all four adapters following the RESEARCH.md examples exactly. Each extends BasePollingAdapter and implements `poll()`.

**github_adapter.py** (GitHubAdapter):
- Config expects: token, repos (list of "owner/repo")
- Polls: open PRs (GET /repos/{owner}/{repo}/pulls?state=open)
- For each PR: external_id="pr:{repo}:{number}", title="PR #{num}: {title}", snippet=body[:100], url=html_url, source_icon="github"
- Tier classification: requested_reviewers -> "review", draft -> "follow_up", else "prep"
- Uses httpx.AsyncClient with Bearer auth and timeout=30

**gitlab_adapter.py** (GitLabAdapter):
- Config expects: token, base_url (default "https://gitlab.com"), project_ids (optional)
- Polls: MRs assigned_to_me and review_requested (GET /api/v4/merge_requests?scope=...)
- external_id="mr:{project_id}:{iid}", title="MR !{iid}: {title}", source_icon="gitlab"
- Tier: review_requested -> "review", assigned -> "prep"
- Uses PRIVATE-TOKEN header auth

**jira_adapter.py** (JiraAdapter):
- Config expects: base_url, email, api_token
- Polls: JQL "assignee = currentUser() OR watcher = currentUser() ORDER BY updated DESC"
- external_id="issue:{key}", title="{key}: {summary}", source_icon="jira"
- Tier: Highest->"now", High->"respond", Medium->"prep", Low/Lowest->"follow_up"
- Uses HTTP basic auth (email:api_token)

**calendar_adapter.py** (GoogleCalendarAdapter):
- Config expects: access_token, refresh_token (manual setup), calendar_id (default "primary")
- Polls: events from now to 24h ahead (GET /calendar/v3/calendars/{id}/events)
- external_id="event:{event_id}", title=event summary, snippet="Starts: {time}", source_icon="calendar"
- Tier: always "prep" (calendar events are planning items)
- Token refresh: if 401, use refresh_token to get new access_token via Google OAuth token endpoint, update source config
- Per research: accept pre-generated tokens; full OAuth flow is Phase 4

Register all four in scheduler.py's get_adapter() registry.

All adapters: wrap httpx calls in try/except, log errors, return empty list on failure (never crash the scheduler job).
  </action>
  <verify>
Run: `cd /c/Users/USER/locus && docker compose exec app python -c "from app.integrations.github_adapter import GitHubAdapter; from app.integrations.gitlab_adapter import GitLabAdapter; from app.integrations.jira_adapter import JiraAdapter; from app.integrations.calendar_adapter import GoogleCalendarAdapter; print('All adapters OK')"`
  </verify>
  <done>All four adapters poll their respective APIs and return IngestPayload-compatible dicts. Error handling prevents adapter failures from crashing the scheduler.</done>
</task>

</tasks>

<verification>
- Scheduler starts in lifespan and queries IntegrationSource table
- Each adapter class is importable and registered in get_adapter()
- Adapters return dicts matching IngestPayload schema
- Error handling: adapter failures are logged, not propagated
- APScheduler adds interval jobs per source's poll_interval_seconds
</verification>

<success_criteria>
Polling infrastructure operational. When IntegrationSource records exist with valid credentials, adapters automatically fetch and ingest items into the feed at configured intervals.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-management-work-feed/02-05-SUMMARY.md`
</output>
