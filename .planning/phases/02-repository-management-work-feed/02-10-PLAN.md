---
phase: 02-repository-management-work-feed
plan: 10
type: execute
wave: 4
depends_on: ["02-06", "02-07", "02-08"]
files_modified:
  - frontend/src/stores/commandPaletteStore.ts
  - frontend/src/components/palette/CommandPalette.tsx
  - frontend/src/components/layout/AppShell.tsx
  - frontend/package.json
autonomous: true

must_haves:
  truths:
    - "Ctrl+K and Ctrl+P both open the command palette"
    - "Palette searches across repos, machines, feed items, tasks, and actions"
    - "Results are grouped with section headers"
    - "Selecting a result navigates to the relevant entity"
  artifacts:
    - path: "frontend/src/components/palette/CommandPalette.tsx"
      provides: "cmdk-based command palette with grouped results"
      contains: "CommandPalette"
    - path: "frontend/src/stores/commandPaletteStore.ts"
      provides: "Palette open/close state"
      contains: "useCommandPaletteStore"
  key_links:
    - from: "frontend/src/components/palette/CommandPalette.tsx"
      to: "/api/search"
      via: "server-side search query"
      pattern: "api/search"
    - from: "frontend/src/components/palette/CommandPalette.tsx"
      to: "multiple stores"
      via: "onSelect dispatches to relevant store"
      pattern: "useRepoStore|useFeedStore|useTaskStore"
---

<objective>
Build the cmdk-powered command palette with Ctrl+K/P trigger, grouped search results, and navigation actions.

Purpose: The command palette is the universal navigation and action surface. Users fuzzy-search across all Phase 2 entities and trigger actions without leaving the keyboard.
Output: Command palette component, keyboard shortcut integration, action dispatching.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-management-work-feed/02-CONTEXT.md
@.planning/phases/02-repository-management-work-feed/02-RESEARCH.md
@frontend/src/components/layout/AppShell.tsx
@frontend/src/stores/panelStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install cmdk and build command palette</name>
  <files>
    frontend/src/stores/commandPaletteStore.ts
    frontend/src/components/palette/CommandPalette.tsx
    frontend/src/components/layout/AppShell.tsx
    frontend/package.json
  </files>
  <action>
Install cmdk: `npm install cmdk`

**commandPaletteStore.ts** -- Zustand store:
- State: isOpen (boolean)
- Actions: open(), close(), toggle()

**CommandPalette.tsx** -- Following RESEARCH.md Pattern 6:
- Use `Command.Dialog` from cmdk for modal behavior
- Open state controlled by commandPaletteStore.isOpen
- Keyboard shortcut: Ctrl+K and Ctrl+P both toggle (per LOCKED: both Ctrl+K and Ctrl+P)
- Register keyboard listener in useEffect (prevent default on both combos)

Search approach: Use server-side search via /api/search?q= for results. Use `shouldFilter={false}` on Command since server does filtering. Debounce input by 200ms before fetching.

Groups (per LOCKED: grouped results with section headers):
1. **Repos**: icon=GitBranch, onSelect -> set selectedRepo in repoStore + expand sidebar
2. **Machines**: icon=Server, onSelect -> navigate to machine
3. **Feed Items**: icon=Inbox, onSelect -> switch to Feed tab + scroll to item
4. **Tasks**: icon=CheckSquare, onSelect -> switch to Board tab + highlight task
5. **Actions**: static list, always shown:
   - "Toggle Sidebar" (Ctrl+B) -> panelStore.toggleSidebar
   - "Toggle Feed Panel" (Ctrl+J) -> panelStore toggle right panel
   - "New Task" -> open task creation flow
   - "Open Terminal" (Ctrl+`) -> focus terminal

Each Command.Item shows: icon, title, subtitle (dimmed, right-aligned), keyboard shortcut hint if applicable.

Style with Tailwind (cmdk is unstyled/headless):
- Overlay: fixed inset-0 bg-black/50 z-50
- Dialog: fixed top-[20%] left-1/2 -translate-x-1/2 w-[560px] bg-dominant rounded-lg shadow-2xl border border-border
- Input: w-full px-4 py-3 bg-transparent text-primary-text text-sm border-b border-border outline-none
- List: max-h-[400px] overflow-y-auto
- Group heading: px-3 py-1.5 text-[10px] uppercase tracking-wider text-muted
- Item: px-3 py-2 flex items-center gap-2 text-sm cursor-pointer hover:bg-hover aria-selected:bg-hover rounded-sm mx-1
- Match highlighting: not needed since server-side search

**AppShell.tsx** -- Add CommandPalette:
- Render `<CommandPalette />` at top level of AppShell (portal-like, outside panel layout)
- Add additional keyboard shortcuts (per LOCKED: VS Code-inspired):
  - Ctrl+` -> focus/open terminal (toggle terminal focus)
  - Ctrl+B -> toggle sidebar (already exists)
  - Ctrl+J -> toggle right panel (add if not done in Plan 06)

Close palette on: Escape, clicking outside, selecting an item.
  </action>
  <verify>
Run: `cd /c/Users/USER/locus/frontend && npm run build 2>&1 | tail -5`
  </verify>
  <done>Command palette opens with Ctrl+K/P, shows grouped search results from all domains, and dispatches navigation/actions on selection. Styled with dark theme Tailwind classes matching app design.</done>
</task>

</tasks>

<verification>
- Ctrl+K opens palette
- Ctrl+P opens palette
- Escape closes palette
- Typing filters results from server
- Results grouped: Repos, Machines, Feed Items, Tasks, Actions
- Selecting a repo navigates to it in sidebar
- Actions (Toggle Sidebar, etc.) execute correctly
- VS Code shortcuts work: Ctrl+B (sidebar), Ctrl+J (feed panel), Ctrl+` (terminal)
</verification>

<success_criteria>
Command palette is the universal navigation surface. All Phase 2 entities searchable. VS Code-inspired keyboard shortcuts operational.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-management-work-feed/02-10-SUMMARY.md`
</output>
