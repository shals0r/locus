---
phase: 02-repository-management-work-feed
plan: 11
type: execute
wave: 6
depends_on: ["02-06", "02-07", "02-08", "02-09", "02-10"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All Phase 2 visual and interaction requirements are verified by the user"
    - "Git sidebar shows repo state, commit timeline, GSD state badges, and git operations work"
    - "Feed shows tiered items with triage actions"
    - "Board shows tasks, start flow works, context strip appears"
    - "Command palette searches all entities"
    - "Diffs render when clicking files/commits"
---

<objective>
Visual and functional verification of the complete Phase 2 UI.

Purpose: Human verification that all locked visual decisions, interaction flows, and data connections work correctly in the running application.
Output: Verified Phase 2 or list of issues to address.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-management-work-feed/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed test data and ensure app is running</name>
  <files></files>
  <action>
Before the visual checkpoint, ensure the app has data to verify against:

1. Ensure Docker services are running (`docker compose up -d`)
2. Create a seed script or use curl commands to:
   - Insert 2-3 test feed items across different tiers (now, respond, review) via POST /api/feed/ingest
   - Create 1-2 test tasks (one in queue, one in active state if possible) via POST /api/tasks/
   - Ensure at least one machine with repos is available (should exist from Phase 1)
3. Verify the frontend loads at http://localhost:5173 (or wherever Vite dev server runs)
4. Verify the app compiles and starts without errors

This ensures the checkpoint has visible data to verify against.
  </action>
  <verify>
curl commands return 200/201 for seeded data. Frontend loads without console errors.
  </verify>
  <done>App running with test data seeded for visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 2 UI: git sidebar with repo state, GSD badges, and operations, feed panel with tiered items and triage, task board with start flow and context strip, diff viewer, and command palette.
  </what-built>
  <how-to-verify>
Open the app at http://localhost:5173 and verify each area:

**1. Git Sidebar (left panel)**
- [ ] Left panel splits into top (repos) and bottom (timeline)
- [ ] Repos are grouped by machine with collapsible headers
- [ ] Each repo shows: branch name, dirty dot (green=clean, orange=dirty), changed count, ahead/behind
- [ ] Repos with .planning/ directory show GSD phase badge (phase name, progress, status)
- [ ] GSD action buttons (Plan/Execute/Verify) appear contextually when clicking GSD badge
- [ ] Clicking a repo shows its commit timeline in the bottom section
- [ ] Branch name is clickable and shows a dropdown with branches + "New branch..."
- [ ] Fetch/Pull/Push buttons show inline spinner and result feedback
- [ ] Repo state updates every ~30 seconds

**2. Feed Panel (right panel, Feed tab)**
- [ ] Right panel is visible at ~340px width
- [ ] Feed/Board tab toggle in header
- [ ] Feed items grouped in 5 tier sections with colored headers
- [ ] Now + Respond expanded, Prep + Follow up collapsed
- [ ] Sticky tier headers on scroll
- [ ] Feed card shows: source icon, title, timestamp, unread dot, snippet
- [ ] Hover on card shows: Quick Promote, Deep Promote, Snooze, Dismiss
- [ ] Snooze shows 4 preset options

**3. Task Board (right panel, Board tab)**
- [ ] Switch to Board tab
- [ ] Three vertical sections: Queue, Active, Done
- [ ] Task cards show tier color accent
- [ ] Queue tasks sorted by tier priority (red/now at top)
- [ ] Start button on queue task opens machine -> repo -> branch picker

**4. Working Session**
- [ ] Start a task: context strip appears above center panel
- [ ] Strip shows: tier dot, task title, "Active" badge
- [ ] Expand strip: see context, source links, Copy context button
- [ ] Complete button on strip works

**5. Diff Viewer**
- [ ] Click a changed file in sidebar -> diff opens in center panel tab
- [ ] Diff shows syntax-highlighted unified view
- [ ] Diff tab is closeable

**6. Command Palette**
- [ ] Ctrl+K opens palette
- [ ] Ctrl+P also opens palette
- [ ] Typing shows grouped results (Repos, Machines, Feed Items, Tasks, Actions)
- [ ] Selecting a result navigates correctly
- [ ] Ctrl+B toggles sidebar, Ctrl+J toggles right panel

**7. Overall**
- [ ] Dark theme consistent across all new components
- [ ] No console errors
- [ ] Responsive to panel resizing
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
All Phase 2 visual and interaction requirements verified by user.
</verification>

<success_criteria>
User confirms all Phase 2 features work as designed. Any issues documented for gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-management-work-feed/02-11-SUMMARY.md`
</output>
