---
phase: 02-repository-management-work-feed
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/git_service.py
  - backend/app/services/feed_service.py
  - backend/app/services/task_service.py
  - backend/app/services/gsd_event_service.py
autonomous: true

must_haves:
  truths:
    - "Git service can retrieve repo status, commit log, changed files, and perform fetch/pull/push/checkout/branch-create for any machine"
    - "Git service can read GSD state (.planning/STATE.md, ROADMAP.md) from any repo"
    - "Feed service can ingest items with dedup, AI-classify tiers via LLM, handle snooze/dismiss/read"
    - "Task service enforces valid state transitions (queue->active, queue->done, active->done, active->queue)"
    - "GSD event service can emit feed items when GSD actions complete"
  artifacts:
    - path: "backend/app/services/git_service.py"
      provides: "Git CLI wrapper routing through machine_registry, including GSD state reading"
      contains: "run_command_on_machine"
    - path: "backend/app/services/feed_service.py"
      provides: "Feed item ingest with upsert dedup, AI-assisted tier classification, CRUD"
      contains: "ingest_item"
    - path: "backend/app/services/task_service.py"
      provides: "Task state machine with valid transitions"
      contains: "VALID_TRANSITIONS"
    - path: "backend/app/services/gsd_event_service.py"
      provides: "GSD event emission as feed items"
      contains: "emit_gsd_event"
  key_links:
    - from: "backend/app/services/git_service.py"
      to: "backend/app/services/machine_registry.py"
      via: "run_command_on_machine"
      pattern: "from app\\.services\\.machine_registry import"
    - from: "backend/app/services/feed_service.py"
      to: "backend/app/models/feed_item.py"
      via: "SQLAlchemy ORM queries"
      pattern: "FeedItem"
    - from: "backend/app/services/feed_service.py"
      to: "classify_tier LLM call"
      via: "httpx call to Claude API or local LLM"
      pattern: "classify_tier"
    - from: "backend/app/services/task_service.py"
      to: "backend/app/models/task.py"
      via: "SQLAlchemy ORM queries"
      pattern: "Task"
    - from: "backend/app/services/gsd_event_service.py"
      to: "backend/app/services/feed_service.py"
      via: "ingest_item call"
      pattern: "feed_service\\.ingest_item"
---

<objective>
Create the four core service layers: git operations (including GSD state reading), feed management (with AI-assisted tier classification), task state machine, and GSD event emission.

Purpose: These services encapsulate all business logic. API routes (Plan 03/04) are thin wrappers around them. Building services first ensures testable business logic separate from HTTP concerns.
Output: 4 service modules with complete business logic for git ops, feed ingest/triage with AI classification, task lifecycle, and GSD event emission.
</objective>

<execution_context>
@C:\Users\USER\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\USER\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-management-work-feed/02-RESEARCH.md
@backend/app/services/machine_registry.py
@backend/app/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create git service layer with GSD state reading</name>
  <files>backend/app/services/git_service.py</files>
  <action>
Create the git service layer following RESEARCH.md Pattern 1 exactly. All git operations run via `run_command_on_machine()` from machine_registry -- NOT GitPython (per research finding: GitPython cannot work for remote repos).

Functions to implement:
- `get_repo_status(machine_id, repo_path) -> dict`: branch, is_dirty, changed_count, ahead, behind. Use `git rev-parse --abbrev-ref HEAD`, `git status --porcelain`, `git rev-list --left-right --count HEAD...@{upstream}` (wrap upstream in try/except for repos without tracking).
- `get_commit_log(machine_id, repo_path, limit=30) -> list[dict]`: NUL-separated format (`%H%x00%s%x00%an%x00%aI`) for safe parsing. Returns list of {sha, message, author, date}.
- `get_changed_files(machine_id, repo_path) -> list[dict]`: Parse `git status --porcelain`. Returns list of {status, path}. Map git status codes: M=modified, A=added, D=deleted, ??=untracked, R=renamed.
- `get_diff_for_file(machine_id, repo_path, file_path) -> str`: `git diff -- <file>` for unstaged, include `--cached` variant for staged.
- `get_commit_diff(machine_id, repo_path, commit_sha) -> str`: `git show --format='' <sha>`.
- `get_file_content_at_ref(machine_id, repo_path, ref, file_path) -> str`: `git show <ref>:<file>`.
- `git_fetch(machine_id, repo_path) -> str`: `git fetch --prune 2>&1`.
- `git_pull(machine_id, repo_path) -> str`: `git pull 2>&1`.
- `git_push(machine_id, repo_path) -> str`: `git push 2>&1`.
- `list_branches(machine_id, repo_path) -> list[dict]`: branch name + is_current using `--format='%(refname:short)%x00%(HEAD)'`.
- `checkout_branch(machine_id, repo_path, branch) -> str`: `git checkout <branch> 2>&1`.
- `create_branch(machine_id, repo_path, branch) -> str`: `git checkout -b <branch> 2>&1`.

**GSD state reading (GIT-05):**
- `get_gsd_state(machine_id, repo_path) -> dict | None`: Checks if `.planning/STATE.md` exists in the repo via `test -f {repo_path}/.planning/STATE.md && echo exists`. If exists, reads it via `cat {repo_path}/.planning/STATE.md`. Parse the markdown to extract: current_phase (string), phase_status (string), pending_todos count, blockers count. Also check for `ROADMAP.md` and parse to get total_phases and completed_phases count. Return dict: `{has_gsd: true, current_phase, phase_status, pending_todos, blockers, total_phases, completed_phases}`. Return `{has_gsd: false}` if no `.planning/` directory.
- `get_gsd_roadmap(machine_id, repo_path) -> str | None`: Returns raw content of `.planning/ROADMAP.md` if it exists, else None. For detailed display.

CRITICAL: Use `shlex.quote()` for ALL user-provided values (repo_path, file_path, branch names) to prevent command injection. Import shlex at top.

For ahead/behind, wrap in try/except -- repos without a tracking upstream should return ahead=0, behind=0 without error.
  </action>
  <verify>
Run: `cd /c/Users/USER/locus && docker compose exec app python -c "from app.services.git_service import get_repo_status, get_commit_log, git_fetch, list_branches, get_gsd_state; print('Git service OK')"`
  </verify>
  <done>All git service functions importable, including GSD state reading. Uses run_command_on_machine for all operations, with shlex.quote for input sanitization.</done>
</task>

<task type="auto">
  <name>Task 2: Create feed service (with AI tier classification), task service, and GSD event service</name>
  <files>
    backend/app/services/feed_service.py
    backend/app/services/task_service.py
    backend/app/services/gsd_event_service.py
  </files>
  <action>
**feed_service.py:**
- `classify_tier(title: str, snippet: str | None, source_type: str) -> str`: AI-assisted tier classification per LOCKED decision ("AI-assisted classification -- Claude analyzes item content and suggests a tier"). Use httpx to call the Anthropic API (or configurable LLM endpoint from settings/env var `LLM_API_KEY` / `LLM_API_URL`). Send a prompt: "Classify this work item into one of these urgency tiers: now, respond, review, prep, follow_up. Title: {title}. Source: {source_type}. Snippet: {snippet}. Respond with ONLY the tier name." Parse the single-word response. If LLM call fails (no API key, timeout, error), fall back to source-type-based heuristic: CI failures->"now", PR reviews->"review", Jira high priority->"respond", calendar->"prep", default->"follow_up". Log the fallback. This ensures the feature degrades gracefully without an LLM key.
- `ingest_item(db, payload: dict) -> FeedItem`: Upsert using PostgreSQL INSERT...ON CONFLICT(source_type, external_id) DO UPDATE. Use SQLAlchemy's `insert().on_conflict_do_update()` with the composite constraint. Map payload fields to FeedItem columns. Store payload's metadata as raw_payload. **If tier_hint is provided, use it directly. If tier_hint is absent, call classify_tier() to get AI-suggested tier.** This implements the locked "AI-assisted classification" decision.
- `get_feed_items(db, include_dismissed=False, tier=None) -> list[FeedItem]`: Query with filters. Exclude snoozed items where snoozed_until > now. Order by tier priority (now=0, respond=1, review=2, prep=3, follow_up=4) then created_at desc.
- `update_feed_item(db, item_id, updates: dict) -> FeedItem`: Update is_read, is_dismissed, snoozed_until, tier.
- `snooze_item(db, item_id, until: datetime) -> FeedItem`: Set snoozed_until.
- `dismiss_item(db, item_id) -> FeedItem`: Set is_dismissed=True.
- `mark_read(db, item_id) -> FeedItem`: Set is_read=True.

Define TIER_PRIORITY dict: {"now": 0, "respond": 1, "review": 2, "prep": 3, "follow_up": 4} for sort ordering.

**task_service.py:**
- Define VALID_TRANSITIONS: queue->{active, done}, active->{done, queue}, done->set() (terminal).
- `create_task(db, data: dict) -> Task`: Create task from dict (quick promote) or with context (deep promote). Set status="queue".
- `promote_feed_item(db, feed_item_id, title, context=None, source_links=None) -> Task`: Create task linked to feed item. Copy tier from feed item.
- `transition_task(db, task_id, new_status, **kwargs) -> Task`: Validate transition against VALID_TRANSITIONS. Raise ValueError for invalid. Set started_at on queue->active (also set machine_id, repo_path, branch from kwargs). Set completed_at on *->done.
- `get_tasks(db, status=None) -> list[Task]`: Query with optional status filter. Order queue by tier priority, active by started_at, done by completed_at desc.
- `update_task(db, task_id, updates: dict) -> Task`: Update title, context.

**gsd_event_service.py (FEED-06):**
Service that emits feed items when GSD actions complete. GSD events come from the git sidebar or command palette when users run GSD commands.

- `emit_gsd_event(db, event_type: str, repo_path: str, machine_id: str, details: dict) -> FeedItem`: Creates a feed item for a GSD event. Maps event types to tiers:
  - "phase_planned" -> source_type="gsd", tier="prep", title="Phase planned: {details.phase_name}"
  - "phase_executed" -> source_type="gsd", tier="review", title="Phase executed: {details.phase_name}"
  - "phase_verified" -> source_type="gsd", tier="follow_up", title="Phase verified: {details.phase_name}"
  - "gap_found" -> source_type="gsd", tier="respond", title="Gaps found in {details.phase_name}"
  - "phase_complete" -> source_type="gsd", tier="follow_up", title="Phase complete: {details.phase_name}"
  Uses external_id="gsd:{event_type}:{repo_path}:{details.phase}" to deduplicate. Calls feed_service.ingest_item() to create the feed item. Calls broadcast_feed_update() for real-time push.

Both services import from app.models and app.database. Use async session patterns matching existing services (auth.py).
  </action>
  <verify>
Run: `cd /c/Users/USER/locus && docker compose exec app python -c "from app.services.feed_service import ingest_item, get_feed_items, classify_tier; from app.services.task_service import VALID_TRANSITIONS, create_task, transition_task; from app.services.gsd_event_service import emit_gsd_event; print('Services OK')"`
  </verify>
  <done>Feed service handles ingest with dedup upsert, AI-assisted tier classification (with graceful fallback), and tier-based ordering. Task service enforces state machine transitions. GSD event service emits feed items for GSD workflow events. All follow existing async session patterns.</done>
</task>

</tasks>

<verification>
- git_service imports and uses run_command_on_machine (not GitPython)
- git_service has get_gsd_state() that reads .planning/STATE.md and ROADMAP.md
- feed_service uses ON CONFLICT for dedup
- feed_service calls classify_tier() for items without tier_hint (AI-assisted per locked decision)
- classify_tier() falls back to heuristic when LLM unavailable
- task_service rejects invalid state transitions
- gsd_event_service.emit_gsd_event() creates feed items via feed_service.ingest_item()
- All functions are async
- shlex.quote used in git_service for user inputs
</verification>

<success_criteria>
All four service layers contain complete business logic. API routes (Plans 03-04) can call these directly without additional logic. AI-assisted tier classification works with graceful fallback. GSD state readable from any repo. GSD events emittable as feed items.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-management-work-feed/02-02-SUMMARY.md`
</output>
