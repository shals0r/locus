/**
 * Right panel placeholder. Will contain the work feed in Phase 2.
 * Starts collapsed by default in Phase 1 (D-06).
 */
export function RightPanel() {
  return <div className="h-full bg-secondary" />;
}
